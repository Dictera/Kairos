---
phase: 13-tab-cleanup-ui-ux
plan: 04
type: execute
wave: 2
depends_on: [13-03]
files_modified:
  - components/muvekkil/muvekkil-form.tsx
  - components/muvekkil/muvekkil-list.tsx
  - components/muvekkil/muvekkil-detail.tsx
  - components/dosya/dosya-list.tsx
  - components/dosya/dosya-detail-tabs.tsx
autonomous: true
requirements:
  - UIUX-01

must_haves:
  truths:
    - "Müvekkil form has IBAN field with TR IBAN validation"
    - "Müvekkil form grouped into sections: Kimlik Bilgileri, İletişim, Adres, Notlar"
    - "Müvekkil list shows IBAN column"
    - "Müvekkil detail shows IBAN"
    - "Dosya list has improved column layout"
    - "Dosya detail pages have improved layout and readability"
  artifacts:
    - path: "components/muvekkil/muvekkil-form.tsx"
      provides: "IBAN field with TR regex validation, grouped sections"
      contains: "İletişim"
    - path: "components/muvekkil/muvekkil-list.tsx"
      provides: "IBAN column in list table"
      contains: "iban"
    - path: "components/muvekkil/muvekkil-detail.tsx"
      provides: "IBAN display in detail page"
      contains: "IBAN"
  key_links:
    - from: "components/muvekkil/muvekkil-form.tsx"
      to: "lib/trpc/routers/muvekkil.ts"
      via: "muvekkilSchema with iban field"
      pattern: "iban"
---

<objective>
Add IBAN field to müvekkil (form, list, detail), improve UI grouping/spacing on müvekkil and dosya pages.

Purpose: Per D-18 through D-24 — müvekkil IBAN field, list/form/detail improvements, dosya list and detail layout polish.
Output: Müvekkil components with IBAN and improved UX; dosya list/detail improvements.
</objective>

<execution_context>
@D:/sigorta-takip/.opencode/get-shit-done/workflows/execute-plan.md
@D:/sigorta-takip/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-tab-cleanup-ui-ux/13-CONTEXT.md
@.planning/phases/13-tab-cleanup-ui-ux/13-RESEARCH.md
@.planning/phases/13-tab-cleanup-ui-ux/13-UI-SPEC.md
@.planning/phases/13-tab-cleanup-ui-ux/13-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add IBAN field and grouping to müvekkil form</name>
  <files>components/muvekkil/muvekkil-form.tsx</files>
  <read_first>
    - components/muvekkil/muvekkil-form.tsx (current file — all lines)
    - .planning/phases/13-tab-cleanup-ui-ux/13-UI-SPEC.md (Müvekkil form grouping spec)
  </read_first>
  <action>
Edit `components/muvekkil/muvekkil-form.tsx`:

1. **Add IBAN to formSchema** (after `telefon`, before `tc_vergi_no`):
```typescript
iban: z.string().regex(/^TR\d{24}$/, 'Geçersiz IBAN formatı (TRXXXXXXXXXXXXXXXXXXXXXXXX)').optional().or(z.literal('')),
```

2. **Add IBAN to defaultValues** (after `telefon`, before `tc_vergi_no`):
```typescript
iban: '',
```

3. **Restructure form into grouped sections** per UI-SPEC D-19. Use `Separator` component and `h3` headings:

   - Group 1: "Kimlik Bilgileri" — Ad, Soyad (side-by-side in 2-col grid), TC No/Vergi No
   - Group 2: "İletişim" — Telefon, IBAN (side-by-side in 2-col grid)
   - Group 3: "Adres" — Adres (Textarea)
   - Group 4: "Notlar" — Notlar (Textarea)

   Each group:
   ```tsx
   <div className="space-y-4">
     <h3 className="text-sm font-semibold">Group Title</h3>
     <Separator />
     {/* form fields */}
   </div>
   ```

4. **Add IBAN form field** in İletişim group:
```tsx
<FormField
  control={form.control}
  name="iban"
  render={({ field }) => (
    <FormItem>
      <FormLabel>IBAN</FormLabel>
      <FormControl>
        <Input placeholder="TRXXXXXXXXXXXXXXXXXXXXXXXX" {...field} />
      </FormControl>
      <FormMessage />
    </FormItem>
  )}
/>
```

5. **Import Separator** from `@/components/ui/separator`
6. **Update edit mode defaultValues** to include `iban: data.iban ?? ''`
7. **Update onSubmit** — IBAN should be passed through (empty string → undefined conversion like other optional fields)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <acceptance_criteria>
    - `muvekkil-form.tsx` contains `iban` field in formSchema with TR IBAN regex
    - `muvekkil-form.tsx` contains grouped sections with headings: Kimlik Bilgileri, İletişim, Adres, Notlar
    - `muvekkil-form.tsx` contains IBAN Input with placeholder "TRXXXXXXXXXXXXXXXXXXXXXXXX"
    - `muvekkil-form.tsx` imports Separator component
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Müvekkil form updated with IBAN field, TR IBAN validation, and grouped sections.</done>
</task>

<task type="auto">
  <name>Task 2: Add IBAN column to müvekkil list and IBAN to müvekkil detail</name>
  <files>components/muvekkil/muvekkil-list.tsx, components/muvekkil/muvekkil-detail.tsx, lib/trpc/routers/muvekkil.ts</files>
  <read_first>
    - components/muvekkil/muvekkil-list.tsx (current columns and table)
    - components/muvekkil/muvekkil-detail.tsx (current detail page structure)
    - lib/trpc/routers/muvekkil.ts (list query SELECT fields)
  </read_first>
  <action>
1. **lib/trpc/routers/muvekkil.ts** — Verify `muvekkil.list` query includes `iban: muvekkil.iban` in the SELECT. Also update `muvekkil.getById` to return IBAN (it should already since Drizzle `findFirst` returns all columns).

2. **components/muvekkil/muvekkil-list.tsx** — Add IBAN column to the table. Place it after the Telefon column:
```tsx
<TableHead>IBAN</TableHead>
```
```tsx
<TableCell>{muvekkil.iban ?? '—'}</TableCell>
```

3. **components/muvekkil/muvekkil-detail.tsx** — Add IBAN display in the detail page, after the Telefon row:
```tsx
<Row label="IBAN" value={muvekkil.iban ?? '—'} />
```

Minor UI improvements per D-18 and D-20: Review column widths and spacing in the list. Consider making the list more compact by reducing unnecessary padding. For the detail page, ensure IBAN appears alongside other contact information.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <acceptance_criteria>
    - `muvekkil-list.tsx` shows IBAN column in table
    - `muvekkil-detail.tsx` shows IBAN value
    - `muvekkil.ts` list query includes `iban` in SELECT
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>IBAN visible in müvekkil list and detail. tRPC query returns IBAN field.</done>
</task>

<task type="auto">
  <name>Task 3: Improve dosya list and dosya detail layout</name>
  <files>components/dosya/dosya-list.tsx, components/dosya/dosya-detail-tabs.tsx</files>
  <read_first>
    - components/dosya/dosya-list.tsx (current columns and layout)
    - components/dosya/dosya-detail-tabs.tsx (current detail page header)
  </read_first>
  <action>
1. **components/dosya/dosya-list.tsx** — Review current column order. Make minor improvements for better readability:
   - Ensure column order is logical: Dosya No, Müvekkil, Tür, Durum, Oluşturma Tarihi
   - Consider adding `hasar_dosya_no` as an optional info field if it makes sense for the list view (D-22)
   - Adjust column widths if needed for compact layout

2. **components/dosya/dosya-detail-tabs.tsx** — Minor layout improvements for readability:
   - Ensure proper spacing in the header area
   - Verify that all tabs render correctly after the Notlar tab update from Plan 02

These are minor UI polish changes per D-22 and D-24. Focus on consistency and readability rather than major restructuring.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <acceptance_criteria>
    - `dosya-list.tsx` displays correctly with current column layout
    - `dosya-detail-tabs.tsx` renders all 6 tabs correctly (Genel Bilgiler, Yargılama Süreci, Belgeler, Notlar, Karşı Taraflar, Dosya Finansı)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Dosya list and detail pages reviewed and polished.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client form → tRPC API | IBAN validated with TR regex pattern |
| tRPC API → SQLite | Parameterized queries via Drizzle prevent SQL injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Tampering | IBAN input | mitigate | Zod regex `/^TR\d{24}$/` validates format at tRPC entry point |
| T-13-08 | Information disclosure | IBAN visible in list | accept | Single-user app, IBAN is not high-sensitivity in this context |
</threat_model>

<verification>
1. Run `npm test -- --run` — all tests pass
2. Run `npx tsc --noEmit` — TypeScript compiles
3. Manual browser test:
   - Navigate to müvekkil create → verify grouped form with IBAN field
   - Navigate to müvekkil detail → verify IBAN displayed
   - Navigate to müvekkil list → verify IBAN column
   - Edit müvekkil → verify IBAN pre-populates in edit mode
   - Enter invalid IBAN → verify validation error "Geçersiz IBAN formatı"
   - Navigate to dosya list → verify column layout
</verification>

<success_criteria>
1. Müvekkil form has IBAN field with TR IBAN validation (D-21)
2. Müvekkil form grouped into Kimlik Bilgileri, İletişim, Adres, Notlar (D-19)
3. Müvekkil list shows IBAN column (D-18)
4. Müvekkil detail shows IBAN (D-20)
5. Dosya list and detail pages render correctly (D-22, D-24)
6. `npm test -- --run` passes
7. `npx tsc --noEmit` passes
</success_criteria>

<output>
After completion, create `.planning/phases/13-tab-cleanup-ui-ux/13-04-SUMMARY.md`
</output>