---
phase: 11-m-vekkil-email-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/02-schema.test.ts
autonomous: false
requirements:
  - MUVEK-05
must_haves:
  truths:
    - "tests/02-schema.test.ts has a real column-verification test for muvekkil table"
    - "Test explicitly asserts muvekkil table does NOT have email column"
    - "Test asserts muvekkil table HAS all expected columns: id, ad, soyad, telefon, tc_vergi_no, adres, notlar, created_at, updated_at"
    - "Müvekkil create/edit form has no email input field (manual verification)"
    - "Müvekkil list displays without email column (manual verification)"
  artifacts:
    - path: "tests/02-schema.test.ts"
      provides: "Column-verification test for muvekkil schema"
      contains: "not.toHaveProperty('email')"
  key_links:
    - from: "tests/02-schema.test.ts"
      to: "lib/schema.ts"
      via: "import { muvekkil } from '@/lib/schema'"
      pattern: "import.*muvekkil.*from.*@/lib/schema"
user_setup: []
---

<objective>
Replace the stale `it.todo` in tests/02-schema.test.ts with a real column-verification test that provides regression protection against accidentally re-adding email to the muvekkil table, then manually verify all 4 success criteria.

Purpose: The existing `it.todo` on line 4 is stale — it lists `email` as a muvekkil column, which is incorrect. Per D-03, replace it with a real assertion test. Per D-04, verify all success criteria.

Output: Working regression test + verified success criteria.
</objective>

<execution_context>
@D:/sigorta-takip/.opencode/get-shit-done/workflows/execute-plan.md
@D:/sigorta-takip/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-m-vekkil-email-removal/11-CONTEXT.md
@.planning/phases/11-m-vekkil-email-removal/11-RESEARCH.md
@.planning/phases/11-m-vekkil-email-removal/11-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/schema.ts (line 123-133):
```typescript
export const muvekkil = sqliteTable('muvekkil', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  ad: text('ad').notNull(),
  soyad: text('soyad').notNull(),
  telefon: text('telefon'),
  tc_vergi_no: text('tc_vergi_no'),
  adres: text('adres'),
  notlar: text('notlar'),
  created_at: text('created_at').notNull().default(sql`(datetime('now'))`),
  updated_at: text('updated_at').notNull().default(sql`(datetime('now'))`),
})
// NO email column — must be verified by test
```

From tests/lib/schema.test.ts (established pattern to follow):
```typescript
import { describe, it, expect } from 'vitest'
import { taraf } from '@/lib/schema'

describe('taraf table schema', () => {
  it('has surucu_ad column', () => {
    expect(taraf).toHaveProperty('surucu_ad')
  })
  // ... more column checks
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace stale it.todo with real column-verification test</name>
  <files>tests/02-schema.test.ts</files>
  <read_first>
    - tests/02-schema.test.ts (the file being modified — contains stale it.todo on line 4)
    - tests/lib/schema.test.ts (established pattern for column-verification tests using toHaveProperty)
    - lib/schema.ts (source of truth for muvekkil table columns — NO email column)
  </read_first>
  <acceptance_criteria>
    - tests/02-schema.test.ts contains `import { muvekkil } from '@/lib/schema'`
    - tests/02-schema.test.ts contains `expect(muvekkil).toHaveProperty('id')` or equivalent positive assertion
    - tests/02-schema.test.ts contains `expect(muvekkil).not.toHaveProperty('email')` explicit negative assertion
    - `npx vitest run tests/02-schema.test.ts` exits with code 0
    - No `it.todo` for muvekkil columns remains in the file (line 4 replaced with real test)
    - All other it.todo entries (dosya, taraf, sigorta_turu, etc.) still exist unchanged
  </acceptance_criteria>
  <behavior>
    - Test: muvekkil table has all expected columns (id, ad, soyad, telefon, tc_vergi_no, adres, notlar, created_at, updated_at)
    - Test: muvekkil table does NOT have email column (regression protection)
    - Existing it.todo entries for other tables (dosya, taraf, etc.) remain unchanged
    - The test follows the established pattern from tests/lib/schema.test.ts using toHaveProperty
  </behavior>
  <action>
Per D-03: Replace the stale `it.todo` on line 4 of `tests/02-schema.test.ts` with a real column-verification test.

The current line 4 is:
```
  it.todo('muvekkil table has correct columns: id, ad, soyad, telefon, email, tc_vergi_no, adres, created_at, updated_at')
```

**Note:** This line incorrectly lists `email` as a muvekkil column — it must be removed and replaced with a real test that explicitly asserts NO email column.

Replace the `it.todo` on line 4 with a real test following the established pattern in `tests/lib/schema.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import { muvekkil } from '@/lib/schema'

describe('Schema: muvekkil table', () => {
  it('muvekkil table has correct columns and no email', () => {
    const expectedColumns = ['id', 'ad', 'soyad', 'telefon', 'tc_vergi_no', 'adres', 'notlar', 'created_at', 'updated_at']
    for (const col of expectedColumns) {
      expect(muvekkil).toHaveProperty(col)
    }
    // Explicit negative assertion — regression protection against re-adding email
    expect(muvekkil).not.toHaveProperty('email')
  })
})
```

**IMPORTANT:**
- Keep all other `it.todo` entries in the file unchanged (lines 5-9 for other describe blocks, lines 12-19 for tRPC router tests, lines 21-28 for ayarlar, lines 29-35 for dosya)
- Only replace line 4 — the stale muvekkil columns `it.todo`
- Add `import { muvekkil } from '@/lib/schema'` to the import section at the top of the file
- The `describe('Schema: muvekkil table', ...)` block already exists — just replace the single `it.todo` inside it

After the edit, the first `describe` block should look like:
```typescript
describe('Schema: muvekkil table', () => {
  it('muvekkil table has correct columns and no email', () => {
    const expectedColumns = ['id', 'ad', 'soyad', 'telefon', 'tc_vergi_no', 'adres', 'notlar', 'created_at', 'updated_at']
    for (const col of expectedColumns) {
      expect(muvekkil).toHaveProperty(col)
    }
    expect(muvekkil).not.toHaveProperty('email')
  })
  it.todo('dosya table has FK to muvekkil (muvekkil_id NOT NULL)')
  it.todo('taraf table has FK to dosya with ON DELETE CASCADE')
  it.todo('sigorta_turu table exists with id and ad columns')
  it.todo('sigorta_sirketi table exists with id and ad columns')
  it.todo('mahkeme table exists with id, ad, sehir columns')
})
```

Run `npx vitest run tests/02-schema.test.ts` to verify the test passes.
  </action>
  <verify>
    <automated>npx vitest run tests/02-schema.test.ts</automated>
  </verify>
  <done>
    - tests/02-schema.test.ts has real test replacing the stale it.todo on line 4
    - Test asserts all 9 expected muvekkil columns (id, ad, soyad, telefon, tc_vergi_no, adres, notlar, created_at, updated_at)
    - Test explicitly asserts muvekkil does NOT have email column
    - All other it.todo entries in the file remain unchanged
    - Test passes: `npx vitest run tests/02-schema.test.ts`
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify tRPC router has no email field</name>
  <files></files>
  <read_first>
    - lib/trpc/routers/muvekkil.ts (tRPC router — verify no email in muvekkilSchema or queries)
    - .planning/phases/11-m-vekkil-email-removal/11-RESEARCH.md (confirmed no email references in codebase)
  </read_first>
  <acceptance_criteria>
    - lib/trpc/routers/muvekkil.ts muvekkilSchema Zod object has exactly 6 fields: ad, soyad, telefon, tc_vergi_no, adres, notlar (no email)
    - lib/trpc/routers/muvekkil.ts list query select clause does NOT include email
    - `grep -r "email" lib/` returns no matches related to muvekkil
    - `grep -r "email" components/` returns no matches in muvekkil context
    - `npx vitest run` full suite exits with code 0
  </acceptance_criteria>
  <action>
Per D-04 success criteria #2: Verify that `muvekkilRouter` create/update procedures accept no email field.

Read `lib/trpc/routers/muvekkil.ts` and confirm:
1. The `muvekkilSchema` Zod object (lines 8-15) has fields: ad, soyad, telefon, tc_vergi_no, adres, notlar — NO email field
2. The `create` procedure uses `muvekkilSchema` as input — email cannot be submitted
3. The `update` procedure uses `muvekkilSchema.extend({ id: ... })` — email cannot be submitted
4. The `list` query selects only: id, ad, soyad, telefon, tc_vergi_no, created_at — NO email
5. The `getById` query returns full muvekkil record via `db.query.muvekkil.findFirst` — NO email in schema

Also verify via grep that no file in the codebase references muvekkil email:
- Search `components/` for any email input field
- Search `lib/` for any email field in muvekkil context
- Search `tests/` for any email reference in muvekkil tests (should only be the new negative assertion)

If all checks pass, commit a verification note (or just verify — no code changes needed if everything is clean, which RESEARCH confirms it is).
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>
    - Confirmed muvekkilSchema has no email field
    - Confirmed create/update procedures use muvekkilSchema (no email possible)
    - Confirmed list query selects no email column
    - Confirmed getById returns no email field
    - No codebase references to muvekkil email in components, lib, or tests (except negative assertion)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all success criteria manually</name>
  <files>N/A — manual verification checkpoint</files>
  <action>Per D-04, verify all 4 success criteria via manual browser testing. This is a human verification checkpoint, not an automated action.</action>
  <verify>Manual browser verification of all 4 success criteria</verify>
  <done>All 4 success criteria verified: (1) no email input in create/edit form, (2) no email in tRPC create/update, (3) existing records display without email, (4) list has no email column</done>
  <what-built>Phase 11 email removal is complete. Drizzle metadata regenerated (Plan 01), schema verification test added (Plan 02 Task 1), tRPC router verified clean (Plan 02 Task 2). All automated tests pass.</what-built>
  <how-to-verify>
    Per D-04, verify the 4 success criteria:

    1. **Müvekkil create/edit form has no email input field:**
       - Navigate to /muvekkiller/new
       - Verify the form has fields: ad*, soyad*, telefon, tc_vergi_no, adres, notlar
       - Verify NO email field exists anywhere in the form

    2. **tRPC muvekkilRouter create/update procedures accept no email field:**
       - Already verified programmatically — muvekkilSchema has no email field
       - Try creating a müvekkil via the form — should succeed with ad, soyad fields filled

    3. **Existing müvekkil records display correctly without email:**
       - Navigate to /muvekkiller (list page)
       - Click on an existing müvekkil to view detail page
       - Verify all fields display correctly: ad, soyad, telefon, tc_vergi_no, adres, notlar
       - Verify NO email field displays

    4. **Müvekkil list displays without email column:**
       - Navigate to /muvekkiller (list page)
       - Verify table columns are: Ad Soyad, Telefon, TC/Vergi No, Bağlı Dosya Sayısı, İşlemler
       - Verify NO email column exists in the table
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase involves no security-sensitive changes — test additions and UI verification only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Info Disclosure | Test assertions | accept | Test file contains schema structure assertions, not sensitive data |

No security-sensitive changes. Adding tests and verifying UI are read-only operations.
</threat_model>

<verification>
- `npx vitest run tests/02-schema.test.ts` passes with real muvekkil column-verification test
- `npx vitest run` full suite passes
- No email field in muvekkil create/edit form (manual)
- No email column in müvekkil list table (manual)
- No email display in müvekkil detail view (manual)
</verification>

<success_criteria>
- Stale it.todo replaced with real column-verification test asserting 9 expected columns and explicitly negating email
- tRPC muvekkilRouter verified to have no email field
- All 4 success criteria verified (2 automated, 2 manual)
- All vitest tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-m-vekkil-email-removal/11-02-SUMMARY.md`
</output>