---
phase: 17-pdf-uretim-motoru
plan: 04
type: execute
completed: "2026-04-21"
---

# Plan 17-04 Summary: Integration Test + Canary Template

## Objective
Validate the entire PDF generation pipeline end-to-end with an integration test that exercises all custom filters, Jinja2 conditional blocks, and Turkish character rendering through a real docxtpl render + LibreOffice convert cycle.

## Tasks Completed

### Task 1: Canary Template Fixture and Integration Test
- **Files:** `scripts/docx-pipeline/test_integration.py`, `scripts/docx-pipeline/fixtures/canary-template.docx`
- Programmatically created a `.docx` canary template using `python-docx` with:
  - All 4 custom Jinja2 filters (`upper_tr`, `lower_tr`, `tr_currency`, `tarih`)
  - Jinja2 conditional block (`{% if durusmalar %}`)
  - Jinja2 loop block (`{% for d in durusmalar %}`)
  - Direct Turkish characters: `çÇğĞıİöÖşŞüÜ İstanbul şirket müvekkil`
  - **Times New Roman font** applied to all paragraphs (user requirement — high Turkish char support)
- Built test context with realistic case data including `ahmet ışık` and `MEHMET ÇELİK`
- Render step: pipes JSON command envelope to `main.py` via stdin, asserts success
- Convert step: calls LibreOffice headless at `C:\Program Files\LibreOffice\program\soffice.exe`, asserts PDF creation
- PDF text verification via `PyPDF2`:
  - `AHMET IŞIK` (upper_tr)
  - `mehmet çelik` (lower_tr)
  - `150.000,00` (tr_currency)
  - `14.02.2026` (tarih)
  - `Duruşma var` (conditional)
  - `İstanbul Adliyesi` (loop)
  - `çÇğĞıİöÖşŞüÜ` (Turkish chars preserved)
- Graceful fallback: if LibreOffice or PyPDF2 missing, test skips with WARNING but still passes
- Temp files cleaned up in `finally` block

### Task 2: Human Verification Checkpoint
- PDF üretimi başarılı; Türkçe karakterler render ve convert sonrası korunuyor.
- Görsel doğrulama kullanıcı onayına sunulmuştur.

## Verification
- `python test_integration.py` → exit 0
- PDF file created and > 0 bytes
- All text extraction assertions pass
- Turkish characters survive full pipeline

## Deviations
- **Times New Roman font** added to canary template per user request during execution (not originally in plan).
- PyPDF2 installed in environment to enable text extraction verification.

## Key Files Created/Modified
| File | Action |
|------|--------|
| `scripts/docx-pipeline/test_integration.py` | Created |
| `scripts/docx-pipeline/fixtures/canary-template.docx` | Created (auto-generated by test) |

## Commits
- `0c68e26` test(17-04): add end-to-end integration test with canary template
- `30af5f4` docs(17-04): complete integration test + canary template plan
