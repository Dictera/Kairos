---
phase: 19-belgeler-ui-ve-sablon-yonetimi-ekrani
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/19-cheat-sheet.test.ts
  - components/degiskenler/cheat-sheet-page.tsx
  - components/ayarlar/cheat-sheet-summary-card.tsx
  - app/(dashboard)/ayarlar/degiskenler/page.tsx
  - components/ayarlar/ayarlar-page.tsx
autonomous: true
requirements: [BUI-08, BUI-09]
tags: [ui, ayarlar, cheat-sheet, variable-registry, static, server-component]

must_haves:
  truths:
    - "Ayarlar sayfasında 'Değişken Listesi' başlıklı özet kart görünür ve 'Tüm değişkenleri gör' butonu ile /ayarlar/degiskenler sayfasına yönlendirir."
    - "/ayarlar/degiskenler sayfası VARIABLE_REGISTRY tüm değişkenlerini tab'a göre gruplanmış olarak gösterir."
    - "Her değişken {{ path }} monospace formatında ve Türkçe açıklaması ile birlikte listelenir."
    - "Sayfa alt kısmında Jinja2 filtreleri (tr_currency, tarih, upper_tr, lower_tr) açıklamaları ile listelenir."
    - "Cheat-sheet sayfası Server Component'tır (no 'use client' directive) — static render, sıfır runtime cost."
  artifacts:
    - path: "components/degiskenler/cheat-sheet-page.tsx"
      provides: "Full reference page grouped by tab + Jinja2 filter section"
      min_lines: 80
    - path: "components/ayarlar/cheat-sheet-summary-card.tsx"
      provides: "Card with link to full cheat-sheet page"
      min_lines: 20
    - path: "app/(dashboard)/ayarlar/degiskenler/page.tsx"
      provides: "Next.js route mounting CheatSheetPage"
      min_lines: 5
    - path: "components/ayarlar/ayarlar-page.tsx"
      provides: "CheatSheetSummaryCard mounted after SablonYonetimiSection"
      contains: "<CheatSheetSummaryCard"
    - path: "tests/19-cheat-sheet.test.ts"
      provides: "Structure assertions for BUI-08, BUI-09, D-13"
  key_links:
    - from: "components/degiskenler/cheat-sheet-page.tsx"
      to: "lib/docx/variable-registry.ts"
      via: "import { VARIABLE_REGISTRY, VariableInfo } + group by tab"
      pattern: "VARIABLE_REGISTRY"
    - from: "components/ayarlar/cheat-sheet-summary-card.tsx"
      to: "/ayarlar/degiskenler"
      via: "<Link href='/ayarlar/degiskenler'>"
      pattern: "/ayarlar/degiskenler"
    - from: "app/(dashboard)/ayarlar/degiskenler/page.tsx"
      to: "components/degiskenler/cheat-sheet-page.tsx"
      via: "import + default export render"
      pattern: "CheatSheetPage"
    - from: "components/ayarlar/ayarlar-page.tsx"
      to: "components/ayarlar/cheat-sheet-summary-card.tsx"
      via: "import + JSX mount"
      pattern: "CheatSheetSummaryCard"
---

<objective>
Build the "Değişken Listesi" cheat-sheet: a static Server Component full-page reference at `/ayarlar/degiskenler` grouped by tab, plus a compact summary card on the Ayarlar page that links to it. The cheat-sheet is the single human-readable surface for the `VARIABLE_REGISTRY` source of truth — lawyers reference it while authoring `.docx` templates in Word.

Purpose: Satisfies BUI-08 (cheat-sheet page + summary card) and BUI-09 (registry consumption). Decisions D-08 (both summary card AND full page), D-09 (copy-paste friendly monospace), D-10 (summary is link-only, no inline list), D-13 (Jinja2 filter docs inline with global filter section).
Output: 2 new components, 1 new route, 1 enhanced page, 1 new test file.
</objective>

<execution_context>
@D:/sigorta-takip/.claude/get-shit-done/workflows/execute-plan.md
@D:/sigorta-takip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-belgeler-ui-ve-sablon-yonetimi-ekrani/19-CONTEXT.md
@.planning/phases/19-belgeler-ui-ve-sablon-yonetimi-ekrani/19-RESEARCH.md
@.planning/phases/19-belgeler-ui-ve-sablon-yonetimi-ekrani/19-PATTERNS.md
@.planning/phases/19-belgeler-ui-ve-sablon-yonetimi-ekrani/19-UI-SPEC.md

@components/ayarlar/ayarlar-page.tsx
@app/(dashboard)/ayarlar/page.tsx
@lib/docx/variable-registry.ts

<interfaces>
From lib/docx/variable-registry.ts:
```typescript
export interface VariableInfo {
  path: string   // e.g., 'muvekkil.ad', 'durusmalar[0].tarih'
  tab: string    // one of: 'genel' | 'taraflar' | 'surec' | 'durusmalar' | 'finans' | 'notlar'
  label: string  // Turkish human-readable label
}
export const VARIABLE_REGISTRY: VariableInfo[]
```
`VariableInfo` does NOT have a `filters` field. Jinja2 filter documentation is rendered as a dedicated section below the grouped variable list — not inline per variable — because the registry has no per-variable filter metadata today.

From components/ui/card.tsx:
```typescript
// Exports: Card, CardHeader, CardTitle, CardContent
```

From components/ui/button.tsx:
```typescript
// Exports: Button (supports asChild + variant="outline")
```

Turkish tab labels (mirror lib/trpc/routers/pdf.ts tabLabel helper — do NOT duplicate the function, just reuse the mapping):
- genel → Genel
- taraflar → Taraflar
- surec → Süreç
- durusmalar → Duruşmalar
- finans → Finans
- notlar → Notlar
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 structure test (tests/19-cheat-sheet.test.ts)</name>
  <files>tests/19-cheat-sheet.test.ts</files>
  <read_first>
    - tests/16-sablon-yonetimi-section.test.ts (file-read assertion pattern)
    - .planning/phases/19-belgeler-ui-ve-sablon-yonetimi-ekrani/19-UI-SPEC.md §5, §6 (summary card + full page contracts)
    - .planning/phases/19-belgeler-ui-ve-sablon-yonetimi-ekrani/19-PATTERNS.md §cheat-sheet-page.tsx, §cheat-sheet-summary-card.tsx, §ayarlar-page.tsx enhance
  </read_first>
  <behavior>
    Four describe blocks across the four file paths:

    - Suite 1 "CheatSheetPage (BUI-08, BUI-09, D-09)": reads `components/degiskenler/cheat-sheet-page.tsx`
      - Does NOT start with `'use client'` (Server Component)
      - Imports `VARIABLE_REGISTRY` from `@/lib/docx/variable-registry`
      - Contains copy: `Değişken Listesi`, `Şablonlarda kullanılabilen tüm değişkenler ve açıklamaları.`
      - Contains Turkish tab headings: `Genel`, `Taraflar`, `Süreç`, `Duruşmalar`, `Finans`, `Notlar`
      - Contains literal strings: `Jinja2 Filtreler`, `tr_currency`, `tarih`, `upper_tr`, `lower_tr`
      - Contains the filter description strings: `150.000,00 TL`, `14.02.2026`, `ı→I`, `İ→i`
      - Renders `{{ ` and ` }}` monospace syntax around variable paths

    - Suite 2 "CheatSheetSummaryCard (BUI-08 entry, D-10)": reads `components/ayarlar/cheat-sheet-summary-card.tsx`
      - Contains copy: `Değişken Listesi`, `Tüm değişkenleri gör`, `Şablonlarınızda kullanabileceğiniz tüm değişkenlerin listesi ve açıklamaları.`
      - Imports `Link` from `next/link`
      - Has `href="/ayarlar/degiskenler"`
      - Uses shadcn `Card`, `CardHeader`, `CardTitle`, `CardContent`
      - Does NOT inline the variable list (D-10 — link-only)

    - Suite 3 "Cheat-sheet route": reads `app/(dashboard)/ayarlar/degiskenler/page.tsx`
      - Imports `CheatSheetPage` from `@/components/degiskenler/cheat-sheet-page`
      - Has `export default function`

    - Suite 4 "AyarlarPage summary card mount": reads `components/ayarlar/ayarlar-page.tsx`
      - Imports `CheatSheetSummaryCard` from `./cheat-sheet-summary-card`
      - Renders `<CheatSheetSummaryCard />` in JSX
      - Does NOT regress existing mounts (`SablonYonetimiSection`, `PipelineStatus`, `SigortaSirketiSection`)
  </behavior>
  <action>
    Create `tests/19-cheat-sheet.test.ts`:

    ```typescript
    import { describe, it, expect } from 'vitest'
    import { readFileSync } from 'fs'

    const PAGE_PATH = 'components/degiskenler/cheat-sheet-page.tsx'
    const CARD_PATH = 'components/ayarlar/cheat-sheet-summary-card.tsx'
    const ROUTE_PATH = 'app/(dashboard)/ayarlar/degiskenler/page.tsx'
    const AYARLAR_PATH = 'components/ayarlar/ayarlar-page.tsx'

    describe('CheatSheetPage (BUI-08, BUI-09, D-09)', () => {
      const src = readFileSync(PAGE_PATH, 'utf-8')
      it('is a Server Component (no use client)', () => {
        expect(src.startsWith("'use client'")).toBe(false)
      })
      it('imports VARIABLE_REGISTRY', () => {
        expect(src).toMatch(/import\s*\{[^}]*VARIABLE_REGISTRY[^}]*\}\s*from\s*['"]@\/lib\/docx\/variable-registry['"]/)
      })
      it('renders page heading and subtitle', () => {
        expect(src).toContain('Değişken Listesi')
        expect(src).toContain('Şablonlarda kullanılabilen tüm değişkenler ve açıklamaları.')
      })
      it('renders all Turkish tab group headings', () => {
        expect(src).toContain('Genel')
        expect(src).toContain('Taraflar')
        expect(src).toContain('Süreç')
        expect(src).toContain('Duruşmalar')
        expect(src).toContain('Finans')
        expect(src).toContain('Notlar')
      })
      it('renders Jinja2 filter section (D-13)', () => {
        expect(src).toContain('Jinja2 Filtreler')
        expect(src).toContain('tr_currency')
        expect(src).toContain('tarih')
        expect(src).toContain('upper_tr')
        expect(src).toContain('lower_tr')
      })
      it('includes filter description examples', () => {
        expect(src).toContain('150.000,00 TL')
        expect(src).toContain('14.02.2026')
        expect(src).toContain('ı→I')
        expect(src).toContain('İ→i')
      })
    })

    describe('CheatSheetSummaryCard (BUI-08 entry, D-10)', () => {
      const src = readFileSync(CARD_PATH, 'utf-8')
      it('contains card title and link copy', () => {
        expect(src).toContain('Değişken Listesi')
        expect(src).toContain('Tüm değişkenleri gör')
        expect(src).toContain('Şablonlarınızda kullanabileceğiniz tüm değişkenlerin listesi ve açıklamaları.')
      })
      it('imports Link from next/link', () => {
        expect(src).toMatch(/import\s+Link\s+from\s+['"]next\/link['"]/)
      })
      it('links to cheat-sheet route', () => {
        expect(src).toMatch(/href=['"]\/ayarlar\/degiskenler['"]/)
      })
      it('uses shadcn Card primitives', () => {
        expect(src).toMatch(/from\s*['"]@\/components\/ui\/card['"]/)
      })
      it('does NOT inline the variable list (D-10 link-only)', () => {
        // No VARIABLE_REGISTRY import or usage in summary card
        expect(src).not.toMatch(/VARIABLE_REGISTRY/)
      })
    })

    describe('Cheat-sheet route', () => {
      const src = readFileSync(ROUTE_PATH, 'utf-8')
      it('imports CheatSheetPage', () => {
        expect(src).toMatch(/import\s*\{\s*CheatSheetPage\s*\}\s*from\s*['"]@\/components\/degiskenler\/cheat-sheet-page['"]/)
      })
      it('has default export', () => {
        expect(src).toMatch(/export\s+default\s+function/)
      })
    })

    describe('AyarlarPage summary card mount', () => {
      const src = readFileSync(AYARLAR_PATH, 'utf-8')
      it('imports CheatSheetSummaryCard', () => {
        expect(src).toMatch(/import\s*\{\s*CheatSheetSummaryCard\s*\}\s*from\s*['"]\.\/cheat-sheet-summary-card['"]/)
      })
      it('renders CheatSheetSummaryCard in JSX', () => {
        expect(src).toMatch(/<CheatSheetSummaryCard\s*\/>/)
      })
      it('does not regress existing sections', () => {
        expect(src).toContain('SablonYonetimiSection')
        expect(src).toContain('SigortaSirketiSection')
        expect(src).toContain('PipelineStatus')
      })
    })
    ```
  </action>
  <verify>
    <automated>npm test -- tests/19-cheat-sheet.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/19-cheat-sheet.test.ts` exists.
    - `grep -c "describe(" tests/19-cheat-sheet.test.ts` returns `4`.
    - `grep -c "it(" tests/19-cheat-sheet.test.ts` returns ≥ `17`.
    - Vitest discovers the suite; RED state acceptable until Tasks 2–5 complete.
  </acceptance_criteria>
  <done>Test file exists, Vitest discovers 4 suites; suites go RED pending implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build CheatSheetPage Server Component</name>
  <files>components/degiskenler/cheat-sheet-page.tsx</files>
  <read_first>
    - lib/docx/variable-registry.ts (full file — need all tabs and variables)
    - lib/trpc/routers/pdf.ts lines 18–28 (tabLabel mapping — Turkish labels to reuse)
    - .planning/phases/19-belgeler-ui-ve-sablon-yonetimi-ekrani/19-UI-SPEC.md §6 (exact JSX contract)
    - .planning/phases/19-belgeler-ui-ve-sablon-yonetimi-ekrani/19-PATTERNS.md §cheat-sheet-page.tsx
  </read_first>
  <behavior>
    Static Server Component (no `'use client'` directive, no hooks, no queries) exporting `CheatSheetPage`.

    Layout: `<div className="p-6 space-y-8 max-w-4xl">` containing:
    1. Header block: `<h1 className="text-xl font-semibold">Değişken Listesi</h1>` + `<p className="text-sm text-muted-foreground">Şablonlarda kullanılabilen tüm değişkenler ve açıklamaları.</p>`
    2. One `<section>` per tab in this fixed order: `genel`, `taraflar`, `surec`, `durusmalar`, `finans`, `notlar`. Each section has:
       - `<h2 className="text-base font-semibold">{Turkish tab label}</h2>`
       - `<div className="border rounded-lg divide-y">` with one row per variable. Row: `<code className="text-sm font-mono bg-muted px-1.5 py-0.5 rounded">{{ path }}</code>` + `<p className="text-sm text-muted-foreground">{label}</p>`.
    3. Final section "Jinja2 Filtreler" with 4 entries (one per filter) using same Card-divider pattern. Descriptions:
       - `tr_currency` — `Türk Lirası formatında para birimi (örn. 150.000,00 TL)`
       - `tarih` — `Tarih formatı (örn. 14.02.2026)`
       - `upper_tr` — `Büyük harf (Türkçe karakter duyarlı: ı→I, İ→i)`
       - `lower_tr` — `Küçük harf (Türkçe karakter duyarlı)`

    Tab label mapping inlined (single const — do not import `tabLabel` from pdf router; replicate the mapping to keep router and UI decoupled).

    Variables are partitioned into sections by iterating `VARIABLE_REGISTRY` and filtering by `v.tab === tabKey`. Preserve the order inside the registry (do NOT re-sort). If a tab has zero entries, skip the section.
  </behavior>
  <action>
    Create `components/degiskenler/cheat-sheet-page.tsx`:

    ```typescript
    import { VARIABLE_REGISTRY, type VariableInfo } from '@/lib/docx/variable-registry'

    const TAB_ORDER: Array<{ key: string; label: string }> = [
      { key: 'genel', label: 'Genel' },
      { key: 'taraflar', label: 'Taraflar' },
      { key: 'surec', label: 'Süreç' },
      { key: 'durusmalar', label: 'Duruşmalar' },
      { key: 'finans', label: 'Finans' },
      { key: 'notlar', label: 'Notlar' },
    ]

    const JINJA_FILTERS: Array<{ name: string; description: string }> = [
      { name: 'tr_currency', description: 'Türk Lirası formatında para birimi (örn. 150.000,00 TL)' },
      { name: 'tarih', description: 'Tarih formatı (örn. 14.02.2026)' },
      { name: 'upper_tr', description: 'Büyük harf (Türkçe karakter duyarlı: ı→I, İ→i)' },
      { name: 'lower_tr', description: 'Küçük harf (Türkçe karakter duyarlı)' },
    ]

    function groupByTab(vars: VariableInfo[]): Record<string, VariableInfo[]> {
      return vars.reduce<Record<string, VariableInfo[]>>((acc, v) => {
        ;(acc[v.tab] ||= []).push(v)
        return acc
      }, {})
    }

    export function CheatSheetPage() {
      const grouped = groupByTab(VARIABLE_REGISTRY)

      return (
        <div className="p-6 space-y-8 max-w-4xl">
          <div>
            <h1 className="text-xl font-semibold">Değişken Listesi</h1>
            <p className="text-sm text-muted-foreground">
              Şablonlarda kullanılabilen tüm değişkenler ve açıklamaları.
            </p>
          </div>

          {TAB_ORDER.map(({ key, label }) => {
            const vars = grouped[key] ?? []
            if (vars.length === 0) return null
            return (
              <section key={key} className="space-y-3">
                <h2 className="text-base font-semibold">{label}</h2>
                <div className="border rounded-lg divide-y">
                  {vars.map((v) => (
                    <div key={v.path} className="flex items-start justify-between gap-4 p-3">
                      <code className="text-sm font-mono bg-muted px-1.5 py-0.5 rounded">
                        {'{{ '}{v.path}{' }}'}
                      </code>
                      <p className="text-sm text-muted-foreground text-right flex-shrink-0 max-w-[50%]">
                        {v.label}
                      </p>
                    </div>
                  ))}
                </div>
              </section>
            )
          })}

          <section className="space-y-3">
            <h2 className="text-base font-semibold">Jinja2 Filtreler</h2>
            <div className="border rounded-lg divide-y">
              {JINJA_FILTERS.map((f) => (
                <div key={f.name} className="p-3">
                  <code className="text-sm font-mono bg-muted px-1.5 py-0.5 rounded">
                    {f.name}
                  </code>
                  <p className="text-sm text-muted-foreground mt-1">{f.description}</p>
                </div>
              ))}
            </div>
          </section>
        </div>
      )
    }
    ```

    Do NOT add `'use client'`. Do NOT import any hooks. This is a pure static render.
  </action>
  <verify>
    <automated>npm test -- tests/19-cheat-sheet.test.ts --run -t "CheatSheetPage"</automated>
  </verify>
  <acceptance_criteria>
    - File `components/degiskenler/cheat-sheet-page.tsx` exists.
    - First non-empty line is NOT `'use client'`: `head -1 components/degiskenler/cheat-sheet-page.tsx` outputs something other than that directive.
    - `grep -F "export function CheatSheetPage" components/degiskenler/cheat-sheet-page.tsx` returns `1`.
    - `grep -F "VARIABLE_REGISTRY" components/degiskenler/cheat-sheet-page.tsx` returns ≥ 2 matches (import + usage).
    - All 6 Turkish tab labels literal: `Genel`, `Taraflar`, `Süreç`, `Duruşmalar`, `Finans`, `Notlar` each grep returns ≥ 1.
    - All 4 filter names literal: `tr_currency`, `tarih`, `upper_tr`, `lower_tr` each grep returns ≥ 1.
    - Filter description substrings present: `150.000,00 TL`, `14.02.2026`, `ı→I`, `İ→i`.
    - `grep -F "Değişken Listesi" components/degiskenler/cheat-sheet-page.tsx` returns ≥ 1.
    - `grep -F "Jinja2 Filtreler" components/degiskenler/cheat-sheet-page.tsx` returns ≥ 1.
    - No hook imports: `grep -E "(useState|useEffect|useQuery|useMutation)" components/degiskenler/cheat-sheet-page.tsx` returns 0 matches.
    - `npx tsc --noEmit` passes.
    - `npm run lint -- components/degiskenler/cheat-sheet-page.tsx` exits 0.
    - Vitest suite 1 "CheatSheetPage" passes.
  </acceptance_criteria>
  <done>Server Component renders grouped registry + filter section; all suite 1 assertions green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build CheatSheetSummaryCard component</name>
  <files>components/ayarlar/cheat-sheet-summary-card.tsx</files>
  <read_first>
    - components/ayarlar/ayarlar-page.tsx lines 84–123 (Card pattern used for password guide — mirror this exactly)
    - components/dosya/dosya-detail-tabs.tsx line 29 (next/link import pattern)
    - .planning/phases/19-belgeler-ui-ve-sablon-yonetimi-ekrani/19-UI-SPEC.md §5
    - .planning/phases/19-belgeler-ui-ve-sablon-yonetimi-ekrani/19-PATTERNS.md §cheat-sheet-summary-card.tsx
  </read_first>
  <behavior>
    Compact card with:
    - Title `Değişken Listesi`
    - Body paragraph: `Şablonlarınızda kullanabileceğiniz tüm değişkenlerin listesi ve açıklamaları.`
    - Outline button `Tüm değişkenleri gör` wrapping `<Link href="/ayarlar/degiskenler">` via `asChild`

    Per D-10: no inline variable list, no registry import. Purely a navigation CTA.
  </behavior>
  <action>
    Create `components/ayarlar/cheat-sheet-summary-card.tsx`:

    ```typescript
    import Link from 'next/link'
    import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
    import { Button } from '@/components/ui/button'

    export function CheatSheetSummaryCard() {
      return (
        <Card>
          <CardHeader>
            <CardTitle className="text-base font-semibold">Değişken Listesi</CardTitle>
          </CardHeader>
          <CardContent className="space-y-3">
            <p className="text-sm text-muted-foreground">
              Şablonlarınızda kullanabileceğiniz tüm değişkenlerin listesi ve açıklamaları.
            </p>
            <Button variant="outline" size="sm" asChild>
              <Link href="/ayarlar/degiskenler">Tüm değişkenleri gör</Link>
            </Button>
          </CardContent>
        </Card>
      )
    }
    ```

    No `'use client'` (pure static — `next/link` works in Server Components). No VARIABLE_REGISTRY import (D-10).
  </action>
  <verify>
    <automated>npm test -- tests/19-cheat-sheet.test.ts --run -t "CheatSheetSummaryCard"</automated>
  </verify>
  <acceptance_criteria>
    - File `components/ayarlar/cheat-sheet-summary-card.tsx` exists.
    - `grep -F "export function CheatSheetSummaryCard" components/ayarlar/cheat-sheet-summary-card.tsx` returns `1`.
    - `grep -F "Değişken Listesi" components/ayarlar/cheat-sheet-summary-card.tsx` returns ≥ 1.
    - `grep -F "Tüm değişkenleri gör" components/ayarlar/cheat-sheet-summary-card.tsx` returns ≥ 1.
    - `grep -F "Şablonlarınızda kullanabileceğiniz tüm değişkenlerin listesi ve açıklamaları." components/ayarlar/cheat-sheet-summary-card.tsx` returns ≥ 1.
    - `grep -F 'from "next/link"' components/ayarlar/cheat-sheet-summary-card.tsx` OR `grep -F "from 'next/link'"` returns ≥ 1.
    - `grep -F "/ayarlar/degiskenler" components/ayarlar/cheat-sheet-summary-card.tsx` returns ≥ 1.
    - `grep -c "VARIABLE_REGISTRY" components/ayarlar/cheat-sheet-summary-card.tsx` returns `0` (D-10 enforced).
    - `npx tsc --noEmit` passes.
    - `npm run lint -- components/ayarlar/cheat-sheet-summary-card.tsx` exits 0.
    - Vitest suite 2 "CheatSheetSummaryCard" passes.
  </acceptance_criteria>
  <done>Summary card renders title + paragraph + link button; no registry duplication; suite 2 green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create Next.js route at /ayarlar/degiskenler</name>
  <files>app/(dashboard)/ayarlar/degiskenler/page.tsx</files>
  <read_first>
    - app/(dashboard)/ayarlar/page.tsx (sibling route — mirror import + default export pattern)
    - .planning/phases/19-belgeler-ui-ve-sablon-yonetimi-ekrani/19-UI-SPEC.md §6
    - .planning/phases/19-belgeler-ui-ve-sablon-yonetimi-ekrani/19-PATTERNS.md §app/(dashboard)/ayarlar/degiskenler/page.tsx
  </read_first>
  <behavior>
    Thin App Router Server Component page that mounts `<CheatSheetPage />`. Per UI-SPEC §6 the CheatSheetPage owns its own padding/heading (`p-6 space-y-8 max-w-4xl`) so the route page does not add a wrapper.
  </behavior>
  <action>
    Create `app/(dashboard)/ayarlar/degiskenler/page.tsx`:

    ```typescript
    import { CheatSheetPage } from '@/components/degiskenler/cheat-sheet-page'

    export default function DegiskenlerRoutePage() {
      return <CheatSheetPage />
    }
    ```

    The parent directory `app/(dashboard)/ayarlar/degiskenler/` does not yet exist — the file creation will create it. No `layout.tsx` needed (inherits from `(dashboard)` group).
  </action>
  <verify>
    <automated>npm test -- tests/19-cheat-sheet.test.ts --run -t "Cheat-sheet route"</automated>
  </verify>
  <acceptance_criteria>
    - File `app/(dashboard)/ayarlar/degiskenler/page.tsx` exists.
    - `grep -F "import { CheatSheetPage } from '@/components/degiskenler/cheat-sheet-page'" "app/(dashboard)/ayarlar/degiskenler/page.tsx"` returns ≥ 1 match.
    - `grep -E "export\s+default\s+function\s+\w+RoutePage" "app/(dashboard)/ayarlar/degiskenler/page.tsx"` returns ≥ 1 match.
    - `grep -F "<CheatSheetPage />" "app/(dashboard)/ayarlar/degiskenler/page.tsx"` returns ≥ 1 match.
    - `npx next build` (or `npx tsc --noEmit` if build is too slow) completes without route-level type errors. Use `npx tsc --noEmit` as the fast path.
    - Vitest suite 3 "Cheat-sheet route" passes.
  </acceptance_criteria>
  <done>Route file exists, exports default page component, mounts CheatSheetPage; suite 3 green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Mount CheatSheetSummaryCard in Ayarlar page</name>
  <files>components/ayarlar/ayarlar-page.tsx</files>
  <read_first>
    - components/ayarlar/ayarlar-page.tsx (whole file — insertion point is between SablonYonetimiSection and PipelineStatus)
    - .planning/phases/19-belgeler-ui-ve-sablon-yonetimi-ekrani/19-PATTERNS.md §ayarlar-page.tsx enhance
  </read_first>
  <behavior>
    Add `CheatSheetSummaryCard` into the existing `<div className="space-y-8">` composition. Insert AFTER `<SablonYonetimiSection />` + its `<Separator />` and BEFORE `<PipelineStatus />`. Wrap with `<Separator />` on the trailing side to maintain the existing rhythm.

    All other existing sections and their logic remain untouched (Mahkemeler CRUD, Sigorta Türleri CRUD, Şifre Değiştirme card, SablonYonetimiSection, PipelineStatus).
  </behavior>
  <action>
    Edit `components/ayarlar/ayarlar-page.tsx`:

    1. Add import alongside existing imports at the top (after the existing `SablonYonetimiSection` import on line 9):
       ```typescript
       import { CheatSheetSummaryCard } from './cheat-sheet-summary-card'
       ```
    2. In the return JSX tree, locate the existing sequence (around lines 127–131):
       ```tsx
       <SablonYonetimiSection />

       <Separator />

       <PipelineStatus />
       ```
       Replace it with:
       ```tsx
       <SablonYonetimiSection />

       <Separator />

       <CheatSheetSummaryCard />

       <Separator />

       <PipelineStatus />
       ```
    3. Do NOT change: any mutation hooks, `AyarlarCrudSection` mounts, Mahkemeler/SigortaTuru logic, `Şifre Değiştirme` Card and its static copy.
  </action>
  <verify>
    <automated>npm test -- tests/19-cheat-sheet.test.ts --run -t "AyarlarPage summary card mount"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "import { CheatSheetSummaryCard } from './cheat-sheet-summary-card'" components/ayarlar/ayarlar-page.tsx` returns ≥ 1 match.
    - `grep -F "<CheatSheetSummaryCard />" components/ayarlar/ayarlar-page.tsx` returns ≥ 1 match.
    - Existing mounts intact: `grep -c "SablonYonetimiSection" components/ayarlar/ayarlar-page.tsx` ≥ 2, `grep -c "PipelineStatus" components/ayarlar/ayarlar-page.tsx` ≥ 2, `grep -c "SigortaSirketiSection" components/ayarlar/ayarlar-page.tsx` ≥ 2.
    - Existing static copy preserved: `grep -F "Şifre Değiştirme" components/ayarlar/ayarlar-page.tsx` returns ≥ 1, `grep -F "APP_PASSWORD" components/ayarlar/ayarlar-page.tsx` returns ≥ 1.
    - Ordering: `<CheatSheetSummaryCard />` must appear AFTER `<SablonYonetimiSection />` and BEFORE `<PipelineStatus />` in the file. Verify via `node -e "const s = require('fs').readFileSync('components/ayarlar/ayarlar-page.tsx','utf8'); const a = s.indexOf('<SablonYonetimiSection'); const b = s.indexOf('<CheatSheetSummaryCard'); const c = s.indexOf('<PipelineStatus'); process.exit((a > -1 && b > a && c > b) ? 0 : 1)"`.
    - `npx tsc --noEmit` passes.
    - `npm run lint -- components/ayarlar/ayarlar-page.tsx` exits 0.
    - Vitest suite 4 "AyarlarPage summary card mount" passes.
    - Full file suite green: `npm test -- tests/19-cheat-sheet.test.ts` all 4 describes pass.
  </acceptance_criteria>
  <done>Summary card mounted in correct position; all Phase 19 cheat-sheet tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time const → rendered HTML | `VARIABLE_REGISTRY` is a TypeScript const resolved at build time; no runtime data crosses a trust boundary in this plan |
| client → route navigation | `<Link href="/ayarlar/degiskenler">` uses Next.js client-side routing — auth enforced by `(dashboard)` group layout, inherited |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-01 | Tampering (XSS via registry content) | CheatSheetPage rendering | accept | `VARIABLE_REGISTRY` is a build-time const authored in source; any malicious content would require a repo commit. React escapes by default. |
| T-19-03-02 | Information Disclosure | Cheat-sheet reveals full variable schema | accept | Desired behavior — this is documentation for the authenticated user. No secrets, only data-shape labels. |
| T-19-03-03 | Open Redirect | `<Link href="/ayarlar/degiskenler">` | mitigate | Href is a hard-coded absolute path literal — not templated from user input. Next.js Link rejects external URLs silently unless explicitly configured. |
| T-19-03-04 | Access Control bypass | `/ayarlar/degiskenler` reachable | mitigate | Route lives under `app/(dashboard)/` group which already enforces session check via its `layout.tsx` (existing pattern inherited — no special handling needed in this route). |
| T-19-03-05 | Denial of Service | Large registry render | mitigate | Current registry ~85 variables. Page is static Server Component — rendered at build time and cached. No runtime query load. |
</threat_model>

<verification>
Automated:
- `npm test -- tests/19-cheat-sheet.test.ts` — 4 suites green (17+ assertions)
- `npm test -- tests/16-sablon-yonetimi-section.test.ts` — no regression
- `npx tsc --noEmit` — clean
- `npm run lint -- components/degiskenler/cheat-sheet-page.tsx components/ayarlar/cheat-sheet-summary-card.tsx "app/(dashboard)/ayarlar/degiskenler/page.tsx" components/ayarlar/ayarlar-page.tsx` — clean

Manual (deferred):
- Navigate to `/ayarlar` → see "Değişken Listesi" card below Şablon Yönetimi, above Pipeline Durumu
- Click `Tüm değişkenleri gör` → navigates to `/ayarlar/degiskenler`
- Full page shows 6 tab sections in order (Genel → Notlar) with `{{ path }}` code + Turkish label
- Scroll to bottom → "Jinja2 Filtreler" section with 4 filter descriptions
- Copy one variable (e.g., `{{ muvekkil.ad }}`) → paste into Word template → upload and test end-to-end
</verification>

<success_criteria>
- BUI-08: ✅ Cheat-sheet page exists at `/ayarlar/degiskenler` + summary card on Ayarlar page (D-08, D-10)
- BUI-09: ✅ CheatSheetPage imports and renders VARIABLE_REGISTRY as single source of truth
- D-13: ✅ Jinja2 filters documented on cheat-sheet page (global section — VariableInfo has no per-variable filter metadata)
- All 4 Vitest suites green; Phase 16 tests not regressed; tsc + lint green
- Page is Server Component (zero `'use client'` cost)
</success_criteria>

<output>
After completion, create `.planning/phases/19-belgeler-ui-ve-sablon-yonetimi-ekrani/19-03-SUMMARY.md`.
</output>
