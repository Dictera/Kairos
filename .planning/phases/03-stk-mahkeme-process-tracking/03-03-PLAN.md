---
phase: 03-stk-mahkeme-process-tracking
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - components/dosya/mahkeme-data-form.tsx
  - components/dosya/durusma-list.tsx
  - components/dosya/durusma-dialog.tsx
  - components/dosya/yargilama-sureci-tab.tsx
autonomous: false
requirements: [SUREC-03, SUREC-04, SUREC-05]

must_haves:
  truths:
    - "Mahkeme stepper renders 8 stages with correct Turkish labels"
    - "User can click 'Ileri Al' to advance Mahkeme stage one step"
    - "All 6 Mahkeme data fields are always visible in a form below the stepper"
    - "User can add a new durusma via dialog with date, time, venue, type, notes"
    - "Durusma list shows all hearings in ascending chronological order"
    - "User can edit an existing durusma via dialog"
    - "User can delete a durusma with confirmation dialog"
    - "All entered data persists across page refreshes"
  artifacts:
    - path: "components/dosya/mahkeme-data-form.tsx"
      provides: "Mahkeme data-point form with 6 fields and independent save"
      exports: ["MahkemeDataForm"]
    - path: "components/dosya/durusma-list.tsx"
      provides: "Chronological durusma table with edit/delete actions"
      exports: ["DurusmaList"]
    - path: "components/dosya/durusma-dialog.tsx"
      provides: "Dialog for adding/editing durusma records"
      exports: ["DurusmaDialog"]
    - path: "components/dosya/yargilama-sureci-tab.tsx"
      provides: "Updated tab with Mahkeme section and Durusma list (replaces Plan 02 stubs)"
  key_links:
    - from: "components/dosya/yargilama-sureci-tab.tsx"
      to: "lib/trpc/routers/surec.ts"
      via: "tRPC mutations: surec.mahkemeIleriAl, surec.updateMahkemeData, surec.durusmaList, surec.durusmaCreate, surec.durusmaUpdate, surec.durusmaDelete"
      pattern: "trpc\\.surec\\."
    - from: "components/dosya/durusma-dialog.tsx"
      to: "lib/trpc/routers/surec.ts"
      via: "durusmaCreate / durusmaUpdate mutations"
      pattern: "surec\\.durusma"
---

<objective>
Build the Mahkeme process tracking UI and the Durusma (hearing) CRUD UI: Mahkeme data form, durusma list table, durusma add/edit dialog, and durusma delete confirmation. Replace the Plan 02 stubs in YargilamaSureciTab with real Mahkeme and Durusma components.

Purpose: Delivers SUREC-03 (Mahkeme stage visualization and advancement), SUREC-04 (Mahkeme data-point entry), and SUREC-05 (durusma CRUD with multiple hearings per file). Completes the entire Yargılama Süreci tab.
Output: Working Mahkeme stepper, Mahkeme data form, durusma list with full CRUD, human-verified.
</objective>

<execution_context>
@D:/sigorta-takip/.claude/get-shit-done/workflows/execute-plan.md
@D:/sigorta-takip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-stk-mahkeme-process-tracking/03-CONTEXT.md
@.planning/phases/03-stk-mahkeme-process-tracking/03-UI-SPEC.md
@.planning/phases/03-stk-mahkeme-process-tracking/03-RESEARCH.md
@.planning/phases/03-stk-mahkeme-process-tracking/03-02-SUMMARY.md

<interfaces>
<!-- Types and components from Plans 01 and 02 that this plan consumes -->

From lib/schema.ts:
```typescript
export const MAHKEME_ASAMALAR = [
  'DAVA_AÇILDI', 'TEBLİGAT', 'CEVAP_DİLEKÇESİ', 'TAHKİKAT',
  'BİLİRKİŞİ', 'KARAR', 'İSTİNAF', 'KESİNLEŞTİ',
] as const
export type MahkemeAsama = typeof MAHKEME_ASAMALAR[number]

export type MahkemeSurecData = {
  asama: MahkemeAsama | null
  esas_no: string | null; karar_no: string | null; mahkeme_adi: string | null
  dava_tarihi: string | null; tebligat_tarihi: string | null; karar_tarihi: string | null
}
export const MAHKEME_ASAMA_LABELS: Record<MahkemeAsama, string>
```

From lib/trpc/routers/surec.ts:
```typescript
// surec.mahkemeIleriAl — input: { dosya_id: number } — returns { asama }
// surec.updateMahkemeData — input: { dosya_id: number, data: { esas_no?, karar_no?, ... } } — returns { success }
// surec.durusmaList — input: { dosya_id: number } — returns array sorted by tarih asc
// surec.durusmaCreate — input: { dosya_id, tarih, saat?, mahkeme_kurum?, tur?, notlar? }
// surec.durusmaUpdate — input: { id, tarih, saat?, mahkeme_kurum?, tur?, notlar? }
// surec.durusmaDelete — input: { id: number }
```

From components/dosya/surec-stepper.tsx (Plan 02):
```typescript
export function SurecStepper<T extends string>(props: {
  stages: readonly T[]; labels: Record<T, string>; current: T | null;
  onAdvance?: () => void; isPending?: boolean;
}): JSX.Element
```

From components/dosya/yargilama-sureci-tab.tsx (Plan 02 — has Mahkeme and Durusma stub sections):
```typescript
export function YargilamaSureciTab(props: {
  dosyaId: number; dosyaTur: string; surecDetayRaw: string | null;
}): JSX.Element
// Contains stub <Card> for "Mahkeme Süreci" and "Duruşmalar" — replace with real components
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MahkemeDataForm + DurusmaDialog + DurusmaList components</name>
  <files>components/dosya/mahkeme-data-form.tsx, components/dosya/durusma-dialog.tsx, components/dosya/durusma-list.tsx</files>
  <read_first>
    - components/dosya/stk-data-form.tsx (follow identical form pattern for MahkemeDataForm — same structure, different fields)
    - components/dosya/karsitaraflar-tab.tsx (mutation + toast + invalidation pattern)
    - components/ui/dialog.tsx (Dialog/DialogContent/DialogHeader/DialogTitle/DialogFooter API)
    - components/ui/alert-dialog.tsx (AlertDialog API for delete confirmation)
    - components/ui/calendar.tsx (Calendar component for date picker)
    - components/ui/popover.tsx (Popover for date picker wrapper)
    - components/ui/select.tsx (Select for mahkeme/kurum dropdown in durusma dialog)
    - components/ui/tooltip.tsx (Tooltip for notes truncation in durusma list)
    - .planning/phases/03-stk-mahkeme-process-tracking/03-UI-SPEC.md (full interaction contracts 4-6, copywriting contract, spacing, color)
  </read_first>
  <action>
**1. Create `components/dosya/mahkeme-data-form.tsx`:**

Follow the EXACT same pattern as `stk-data-form.tsx` but with Mahkeme fields per D-05.

Props:
```typescript
type MahkemeDataFormProps = {
  dosyaId: number
  initialData: MahkemeSurecData | undefined
}
```

Form fields (all optional per D-06):
1. `esas_no` — label "Esas No", type text, Input
2. `karar_no` — label "Karar No", type text, Input
3. `mahkeme_adi` — label "Mahkeme Adı", type text, Input
4. `dava_tarihi` — label "Dava Tarihi", date picker (Calendar+Popover)
5. `tebligat_tarihi` — label "Tebligat Tarihi", date picker
6. `karar_tarihi` — label "Karar Tarihi", date picker

Layout: 2-column grid inside Card with CardHeader "Mahkeme Veri Noktaları" (text-base font-semibold).
"Kaydet" button at bottom, variant="default".
Mutation: `trpc.surec.updateMahkemeData`. onSuccess toast: "Bilgiler kaydedildi." onError toast: "Kaydetme sırasında hata oluştu. Tekrar deneyin."
Invalidate: `queryClient.invalidateQueries({ queryKey: [['dosya', 'getById']] })`

**2. Create `components/dosya/durusma-dialog.tsx`:**

A Dialog component for both add and edit durusma per D-07, D-08.

Props:
```typescript
type DurusmaDialogProps = {
  dosyaId: number
  durusma?: { id: number; tarih: string; saat: string | null; mahkeme_kurum: string | null; tur: string | null; notlar: string | null } | null
  open: boolean
  onOpenChange: (open: boolean) => void
}
```

Dialog title: `durusma ? 'Duruşma Düzenle' : 'Duruşma Ekle'` per copywriting contract.

Form fields (react-hook-form + zodResolver):
1. `tarih` — label "Tarih", date picker (required — Zod `z.string().min(1, 'Tarih seçilmelidir.')`), Calendar+Popover
2. `saat` — label "Saat", text Input, placeholder "09:30"
3. `mahkeme_kurum` — label "Mahkeme/Kurum", text Input (free text — no Select needed since ayarlar lookup is optional per D-08)
4. `tur` — label "Duruşma Türü", text Input, placeholder "esas / ara karar / bilirkişi"
5. `notlar` — label "Notlar", Textarea with 3 rows

Footer buttons (right-aligned per UI-SPEC):
- "Vazgec" — Button variant="outline"
- Add mode: "Durusmayı Kaydet" — Button variant="default"
- Edit mode: "Değişiklikleri Kaydet" — Button variant="default"

Mutations:
- Add: `trpc.surec.durusmaCreate` with `{ dosya_id, ...formValues }`
- Edit: `trpc.surec.durusmaUpdate` with `{ id: durusma.id, ...formValues }`
- onSuccess: close dialog, invalidate `[['surec', 'durusmaList']]` AND `[['dosya', 'getById']]`, toast add: "Duruşma eklendi." / edit: "Duruşma güncellendi."
- onError: `toast.error('İşlem sırasında hata oluştu. Tekrar deneyin.')`

CRITICAL per RESEARCH.md Pitfall 4: call `form.reset(defaultValues)` in `onOpenChange` when `open` goes to `false`. For edit mode, reset to durusma's current values when durusma prop changes.

**3. Create `components/dosya/durusma-list.tsx`:**

Props:
```typescript
type DurusmaListProps = {
  dosyaId: number
}
```

Fetch data: `useQuery(trpc.surec.durusmaList.queryOptions({ dosya_id: dosyaId }))`.

Table columns per UI-SPEC Interaction Contract 4:
| Tarih | Saat | Mahkeme/Kurum | Tür | Notlar | Aksiyonlar |

- Tarih: formatted with `format(parseISO(d.tarih), 'dd.MM.yyyy')` using date-fns
- Saat: raw text or "—" if null/empty
- Mahkeme/Kurum: raw text or "—" if null/empty
- Tür: raw text or "—" if null/empty
- Notlar: truncated to 40 chars with `<Tooltip>` showing full text on hover per UI-SPEC. Show "—" if null/empty.
- Aksiyonlar: two ghost icon-only buttons:
  - Pencil icon (lucide-react `Pencil`) with `aria-label="Duruşmayı düzenle"` — opens DurusmaDialog in edit mode
  - Trash2 icon (lucide-react `Trash2`) with `aria-label="Duruşmayı sil"` — opens AlertDialog for delete confirmation

Delete confirmation AlertDialog per UI-SPEC Interaction Contract 6:
- Title: "Duruşmayı Sil"
- Body: "Bu duruşmayı silmek istediğinizden emin misiniz? Bu işlem geri alınamaz."
- Cancel: "Vazgeç" (variant outline)
- Confirm: "Sil" (variant destructive)
- On confirm: `trpc.surec.durusmaDelete({ id })`, invalidate queries, toast "Duruşma silindi."

Below the table: "Duruşma Ekle" button (variant="default"), left-aligned. Opens DurusmaDialog in add mode.

Empty state per copywriting contract: "Henüz duruşma kaydı yok" heading + "Bu dosyaya ait duruşma kaydı bulunmuyor. Duruşma eklemek için aşağıdaki butonu kullanın." + "Duruşma Ekle" button.

Touch targets for row action buttons: minimum 36px height per UI-SPEC spacing exceptions.

Sort: data comes pre-sorted from tRPC (ascending by tarih). No client-side sort needed.

Table styling: simple `<table>` with Tailwind classes matching the existing pattern in the project. Column headers: text-sm font-semibold. Row cells: text-sm. Empty cell values: `text-muted-foreground`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - components/dosya/mahkeme-data-form.tsx exists and contains `export function MahkemeDataForm`
    - components/dosya/mahkeme-data-form.tsx contains `surec.updateMahkemeData`
    - components/dosya/mahkeme-data-form.tsx contains `esas_no` and `karar_no` and `mahkeme_adi`
    - components/dosya/mahkeme-data-form.tsx contains `Mahkeme Veri Noktaları`
    - components/dosya/durusma-dialog.tsx exists and contains `export function DurusmaDialog`
    - components/dosya/durusma-dialog.tsx contains `Duruşma Ekle` and `Duruşma Düzenle`
    - components/dosya/durusma-dialog.tsx contains `Duruşmayı Kaydet` and `Değişiklikleri Kaydet`
    - components/dosya/durusma-dialog.tsx contains `Vazgeç`
    - components/dosya/durusma-dialog.tsx contains `form.reset`
    - components/dosya/durusma-dialog.tsx contains `surec.durusmaCreate` and `surec.durusmaUpdate`
    - components/dosya/durusma-list.tsx exists and contains `export function DurusmaList`
    - components/dosya/durusma-list.tsx contains `surec.durusmaList`
    - components/dosya/durusma-list.tsx contains `aria-label="Duruşmayı düzenle"`
    - components/dosya/durusma-list.tsx contains `aria-label="Duruşmayı sil"`
    - components/dosya/durusma-list.tsx contains `Henüz duruşma kaydı yok`
    - components/dosya/durusma-list.tsx contains `Duruşmayı Sil`
    - components/dosya/durusma-list.tsx contains `Bu duruşmayı silmek istediğinizden emin misiniz`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>MahkemeDataForm renders 6 fields with save. DurusmaDialog handles add/edit with all 5 fields. DurusmaList shows chronological hearings with edit/delete actions and confirmation. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Mahkeme + Durusma into YargilamaSureciTab, replace stubs</name>
  <files>components/dosya/yargilama-sureci-tab.tsx</files>
  <read_first>
    - components/dosya/yargilama-sureci-tab.tsx (current state from Plan 02 — find stub sections to replace)
    - components/dosya/mahkeme-data-form.tsx (just created — verify export and props)
    - components/dosya/durusma-list.tsx (just created — verify export and props)
    - components/dosya/surec-stepper.tsx (for Mahkeme stepper rendering)
    - lib/schema.ts (MAHKEME_ASAMALAR, MAHKEME_ASAMA_LABELS)
  </read_first>
  <action>
Update `components/dosya/yargilama-sureci-tab.tsx` to replace the Plan 02 stubs with real components.

**Add imports:**
```typescript
import { MahkemeDataForm } from './mahkeme-data-form'
import { DurusmaList } from './durusma-list'
```

**Add Mahkeme Ileri Al mutation (same pattern as STK):**
```typescript
const mahkemeIleriAlMutation = useMutation(
  trpc.surec.mahkemeIleriAl.mutationOptions({
    onSuccess: (data) => {
      queryClient.invalidateQueries({ queryKey: [['dosya', 'getById']] })
      const label = MAHKEME_ASAMA_LABELS[data.asama as keyof typeof MAHKEME_ASAMA_LABELS] ?? data.asama
      toast.success(`Aşama güncellendi: ${label}`)
    },
    onError: () => toast.error('Aşama güncellenirken hata oluştu. Sayfayı yenileyip tekrar deneyin.'),
  })
)
```

**Replace Mahkeme stub Card** (the one containing "Mahkeme süreci bileşeni Plan 03 ile eklenecek") with:
```tsx
<Card>
  <CardHeader>
    <CardTitle>Mahkeme Süreci</CardTitle>
  </CardHeader>
  <CardContent className="space-y-6">
    <SurecStepper
      stages={MAHKEME_ASAMALAR}
      labels={MAHKEME_ASAMA_LABELS}
      current={surecDetay.mahkeme?.asama ?? null}
      onAdvance={() => mahkemeIleriAlMutation.mutate({ dosya_id: dosyaId })}
      isPending={mahkemeIleriAlMutation.isPending}
    />
    <MahkemeDataForm dosyaId={dosyaId} initialData={surecDetay.mahkeme} />
  </CardContent>
</Card>
```

**Replace Durusma stub Card** (the one containing "Duruşma listesi Plan 03 ile eklenecek") with:
```tsx
<Card>
  <CardHeader>
    <CardTitle>Duruşmalar</CardTitle>
  </CardHeader>
  <CardContent>
    <DurusmaList dosyaId={dosyaId} />
  </CardContent>
</Card>
```

**Remove any Plan 02 stub placeholder text.** The final tab should contain only real, functional components.

Verify the section visibility logic remains correct:
- STK files (`dosyaTur === 'STK'`): Show STK section always. Show Mahkeme section only if `surecDetay.mahkeme` exists, otherwise show "Mahkeme Sürecini Başlat" button.
- AT/AH files (`dosyaTur === 'AT'` or `'AH'`): Show Mahkeme section always. Do NOT show STK section.
- Durusma list: Always shown for all file types.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npm run test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - components/dosya/yargilama-sureci-tab.tsx contains `import { MahkemeDataForm }`
    - components/dosya/yargilama-sureci-tab.tsx contains `import { DurusmaList }`
    - components/dosya/yargilama-sureci-tab.tsx contains `mahkemeIleriAlMutation`
    - components/dosya/yargilama-sureci-tab.tsx contains `<MahkemeDataForm dosyaId={dosyaId}`
    - components/dosya/yargilama-sureci-tab.tsx contains `<DurusmaList dosyaId={dosyaId}`
    - components/dosya/yargilama-sureci-tab.tsx does NOT contain `Plan 03 ile eklenecek`
    - components/dosya/yargilama-sureci-tab.tsx contains `surec.mahkemeIleriAl`
    - `npx tsc --noEmit` exits 0
    - `npm run test` all tests pass
  </acceptance_criteria>
  <done>YargilamaSureciTab fully wired: STK stepper+form, Mahkeme stepper+form, Durusma list with CRUD. No placeholder stubs remain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Yargılama Süreci tab with STK process tracking (9 stages + 8 data fields), Mahkeme process tracking (8 stages + 6 data fields), and Duruşma CRUD (add/edit/delete hearings). This delivers all 5 SUREC requirements.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and navigate to http://localhost:3000
    2. Open an STK type dosya detail page -> click "Yargılama Süreci" tab
    3. Verify STK stepper shows 9 stages (Başvuru through Karar Kesinleşti) with empty circles
    4. Click "İleri Al" — verify first stage becomes filled (completed), toast shows "Aşama güncellendi: Başvuru"
    5. Click "İleri Al" again — verify second stage advances
    6. Fill some STK data fields (e.g., başvuru no, raportör adı) and click "Kaydet" — verify toast "Bilgiler kaydedildi"
    7. Refresh page — verify filled fields persist and stage is still at the advanced position
    8. Click "Mahkeme Sürecini Başlat" button — verify Mahkeme section appears with 8 stages
    9. Click "İleri Al" on Mahkeme stepper — verify it advances independently of STK
    10. Fill Mahkeme data fields (esas no, mahkeme adı) and save — verify persistence
    11. In the Duruşmalar section, click "Duruşma Ekle" — verify dialog opens with 5 fields
    12. Fill: Tarih (pick a date), Saat "10:00", Tür "esas", click "Duruşmayı Kaydet" — verify toast "Duruşma eklendi" and hearing appears in list
    13. Add a second hearing with a different date — verify chronological ordering (earlier date first)
    14. Click edit (pencil icon) on a hearing — verify dialog opens pre-filled, change saat, click "Değişiklikleri Kaydet" — verify update persists
    15. Click delete (trash icon) on a hearing — verify confirmation dialog, click "Sil" — verify hearing removed and toast "Duruşma silindi"
    16. Open an AT or AH type dosya — verify only Mahkeme section shows (no STK section)
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 3, or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI form input -> tRPC mutations | Mahkeme data form and durusma dialog send untrusted input |
| Durusma delete action | User can delete any durusma by id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | MahkemeDataForm | mitigate | Server-side Zod validation (mahkemeDataSchema) validates all fields; protectedProcedure enforces auth |
| T-03-09 | Tampering | DurusmaDialog form | mitigate | Zod validation on durusmaCreate/Update schemas: tarih regex, max lengths; protectedProcedure enforces auth |
| T-03-10 | Repudiation | Durusma delete | accept | Single-user localhost app; no audit trail needed. Delete is destructive but user is warned via AlertDialog confirmation |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0
2. `npm run test` — all tests pass (including 03-surec.test.ts, 03-schema.test.ts)
3. Human verification of full STK/Mahkeme/Durusma flows on running app
</verification>

<success_criteria>
- Mahkeme stepper renders 8 stages, advances via Ileri Al, stops at final
- Mahkeme data form shows 6 fields with independent save
- Durusma dialog handles both add and edit with all 5 fields
- Durusma list shows chronological hearings with edit/delete actions
- Delete confirmation matches exact Turkish copywriting from UI-SPEC
- STK files show both sections; AT/AH files show only Mahkeme
- All data persists across page refresh
- Human approves full workflow
</success_criteria>

<output>
After completion, create `.planning/phases/03-stk-mahkeme-process-tracking/03-03-SUMMARY.md`
</output>
