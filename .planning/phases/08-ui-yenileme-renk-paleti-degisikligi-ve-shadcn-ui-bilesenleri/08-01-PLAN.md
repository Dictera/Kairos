---
phase: 08-ui-yenileme-renk-paleti-degisikligi-ve-shadcn-ui-bilesenleri
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/(dashboard)/layout.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "globals.css defines the new Navy + Turuncu palette via CSS custom properties in oklch"
    - "The sidebar token (--sidebar) resolves to navy #032539"
    - "The primary token (--primary) resolves to orange #FA991C"
    - "The background token (--background) resolves to warm white #FBF3F2"
    - "The dashboard main content area uses var(--background) (not hardcoded bg-white)"
  artifacts:
    - path: "app/globals.css"
      provides: "Navy + Turuncu shadcn theme tokens in oklch format"
      contains: "--primary: oklch(0.746 0.174 57"
    - path: "app/(dashboard)/layout.tsx"
      provides: "Dashboard layout shell using theme background token"
      contains: "bg-background"
  key_links:
    - from: "app/globals.css :root"
      to: "shadcn components via Tailwind v4 @theme"
      via: "CSS custom properties (--background, --primary, --sidebar, etc.)"
      pattern: "--primary:\\s*oklch"
    - from: "app/(dashboard)/layout.tsx main element"
      to: "--background token"
      via: "bg-background Tailwind class"
      pattern: "bg-background"
---

<objective>
Replace the teal palette CSS tokens in `app/globals.css` with the Navy + Turuncu palette (D-01, D-04)
and update the dashboard layout to consume the theme background token instead of hardcoded `bg-white`.

Purpose: This is the root of the theme change. Every shadcn component reads from these CSS custom
properties, so updating `globals.css` in isolation (with zero changes to shadcn components themselves)
is what propagates the new colors across the entire app.

Output: Updated `app/globals.css` with oklch values for the new palette, and an updated dashboard
layout that respects the warm white background from D-01.
</objective>

<execution_context>
@D:/sigorta-takip/.claude/get-shit-done/workflows/execute-plan.md
@D:/sigorta-takip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-yenileme-renk-paleti-degisikligi-ve-shadcn-ui-bilesenleri/08-CONTEXT.md

# Reference files the executor MUST read before touching anything
@app/globals.css
@app/(dashboard)/layout.tsx

<palette_reference>
## Navy + Turuncu Palette (Palette 1, D-01)

| Token role              | Hex        | oklch (exact, use verbatim)    | Usage                                   |
|-------------------------|------------|--------------------------------|-----------------------------------------|
| Sidebar background      | #032539    | oklch(0.219 0.044 240)         | Dark navy — sidebar only                |
| Primary / accent orange | #FA991C    | oklch(0.746 0.174 57)          | Primary buttons, active nav, focus ring |
| Secondary steel blue    | #1C768F    | oklch(0.527 0.089 230)         | Passive icons, links, secondary text    |
| Main background         | #FBF3F2    | oklch(0.969 0.008 20)          | Body / main content                     |
| Foreground (dark)       | #032539    | oklch(0.219 0.044 240)         | Text on warm white                      |
| Sidebar foreground      | #FBF3F2    | oklch(0.969 0.008 20)          | Text on navy                            |

Sidebar-accent (D-04): `rgba(250, 153, 28, 0.15)` → in oklch with alpha: `oklch(0.746 0.174 57 / 0.15)`
</palette_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace globals.css CSS tokens with Navy + Turuncu palette</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (current state — see the existing :root block with teal tokens)
    - .planning/phases/08-ui-yenileme-renk-paleti-degisikligi-ve-shadcn-ui-bilesenleri/08-CONTEXT.md (D-04 full token list)
  </read_first>
  <action>
Replace the `:root { ... }` block in `app/globals.css` with exactly the following tokens (other surrounding blocks — `@import`, `.theme`, `*`, `body` — are preserved unchanged). Use the literal oklch values below; do not round or re-compute.

```css
  :root {
    /* Base surfaces — warm white #FBF3F2 */
    --background: oklch(0.969 0.008 20);
    --foreground: oklch(0.219 0.044 240);

    /* Cards / popovers on warm white */
    --card: oklch(0.969 0.008 20);
    --card-foreground: oklch(0.219 0.044 240);
    --popover: oklch(0.969 0.008 20);
    --popover-foreground: oklch(0.219 0.044 240);

    /* Primary — #FA991C orange (D-02) */
    --primary: oklch(0.746 0.174 57);
    --primary-foreground: oklch(0.219 0.044 240);

    /* Secondary — subtle warm-white variant */
    --secondary: oklch(0.93 0.008 20);
    --secondary-foreground: oklch(0.219 0.044 240);

    /* Muted — slightly darker warm white */
    --muted: oklch(0.92 0.006 20);
    --muted-foreground: oklch(0.45 0.02 240);

    /* Accent — #1C768F steel blue */
    --accent: oklch(0.527 0.089 230);
    --accent-foreground: oklch(0.969 0.008 20);

    /* Destructive — unchanged red (protects deadline red signal) */
    --destructive: oklch(0.577 0.245 27.325);

    /* Borders / inputs / focus ring */
    --border: oklch(0.88 0.008 20);
    --input: oklch(0.88 0.008 20);
    --ring: oklch(0.746 0.174 57);

    /* Charts — orange-to-steel-blue ramp for later phases */
    --chart-1: oklch(0.746 0.174 57);
    --chart-2: oklch(0.527 0.089 230);
    --chart-3: oklch(0.219 0.044 240);
    --chart-4: oklch(0.65 0.14 57);
    --chart-5: oklch(0.45 0.08 230);

    --radius: 0.625rem;

    /* Sidebar — navy #032539 (D-01, D-04) */
    --sidebar: oklch(0.219 0.044 240);
    --sidebar-foreground: oklch(0.969 0.008 20);
    --sidebar-primary: oklch(0.746 0.174 57);
    --sidebar-primary-foreground: oklch(0.219 0.044 240);
    --sidebar-accent: oklch(0.746 0.174 57 / 0.15);
    --sidebar-accent-foreground: oklch(0.969 0.008 20);
    --sidebar-border: oklch(0.3 0.04 240);
    --sidebar-ring: oklch(0.746 0.174 57);
  }
```

Rules:
- Do NOT touch `@import "tailwindcss"`, `@import "tw-animate-css"`, `@import "shadcn/tailwind.css"`.
- Do NOT touch the `.theme { ... }` block (font vars).
- Do NOT add a `.dark { ... }` block — dark mode is deferred (see 08-CONTEXT.md Deferred Ideas).
- Do NOT remove the `* { border-color: var(--border); }` rule.
- Do NOT remove the `body { background-color: var(--background); color: var(--foreground); }` rule.
- Every color token must be in `oklch(...)` format. No hex, no rgb, no raw hsl.
- The previous teal values (`#134e4a`, `#f0fdfa`, `rgba(20, 184, 166, ...)`) must NOT appear anywhere in the file after this task.
  </action>
  <verify>
    <automated>grep -c "oklch(0.746 0.174 57)" app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - File `app/globals.css` contains the literal string `--primary: oklch(0.746 0.174 57)`
    - File contains `--background: oklch(0.969 0.008 20)`
    - File contains `--sidebar: oklch(0.219 0.044 240)`
    - File contains `--sidebar-accent: oklch(0.746 0.174 57 / 0.15)`
    - File contains `--ring: oklch(0.746 0.174 57)`
    - File does NOT contain the substring `#134e4a` (old teal)
    - File does NOT contain the substring `#f0fdfa` (old teal foreground)
    - File does NOT contain the substring `rgba(20, 184, 166` (old teal accent)
    - File still contains `@import "tailwindcss"` on line 1
    - File still contains `body {` block with `background-color: var(--background)`
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>globals.css holds the Navy + Turuncu palette in oklch; teal is fully removed; body background consumes --background.</done>
</task>

<task type="auto">
  <name>Task 2: Replace dashboard layout hardcoded bg-white with bg-background</name>
  <files>app/(dashboard)/layout.tsx</files>
  <read_first>
    - app/(dashboard)/layout.tsx (current state — has `bg-white` on the main element)
    - app/globals.css (after Task 1 — confirms --background exists)
  </read_first>
  <action>
In `app/(dashboard)/layout.tsx`, change the `<main>` element's className so that the background comes from the CSS token, not a hardcoded Tailwind color.

Replace:
```tsx
<main className="flex-1 min-h-screen bg-white">
```
with:
```tsx
<main className="flex-1 min-h-screen bg-background text-foreground">
```

Rationale: 08-CONTEXT.md code_context section states the layout's `bg-white` conflicts with the new `#FBF3F2` warm white. Using `bg-background` lets the theme token drive the color, so any future palette swap only requires editing globals.css.

Do NOT modify the `SidebarProvider` or `AppSidebar` imports; do NOT change the default export or its signature; do NOT add any extra wrappers.
  </action>
  <verify>
    <automated>grep -n "bg-background" "app/(dashboard)/layout.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - File `app/(dashboard)/layout.tsx` contains the literal string `className="flex-1 min-h-screen bg-background text-foreground"`
    - File does NOT contain the substring `bg-white`
    - File still contains `import { SidebarProvider } from '@/components/ui/sidebar'`
    - File still contains `import { AppSidebar } from '@/components/app-sidebar'`
    - File still has `export default function DashboardLayout`
    - `npm run lint` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Dashboard main area reads its background from var(--background), removing the last hardcoded `bg-white` in the layout shell.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "#134e4a\|#14b8a6\|#99f6e4\|#f0fdfa\|rgba(20, 184, 166" app/globals.css app/(dashboard)/layout.tsx` → 0
2. `grep -c "oklch" app/globals.css` → at least 25 (every token converted)
3. `npm run lint` exits 0
4. `npx tsc --noEmit` exits 0
5. `npm run dev` starts without CSS errors (manual smoke — deferred to phase verification)
</verification>

<success_criteria>
1. `app/globals.css` contains all Navy + Turuncu tokens in oklch, with `--primary = oklch(0.746 0.174 57)` and `--sidebar = oklch(0.219 0.044 240)`
2. Teal color literals (`#134e4a`, `#14b8a6`, `#99f6e4`, `#f0fdfa`, `rgba(20, 184, 166, ...)`) are completely removed from `app/globals.css` and `app/(dashboard)/layout.tsx`
3. Dashboard main content uses `bg-background text-foreground` classes
4. TypeScript and ESLint pass clean
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-yenileme-renk-paleti-degisikligi-ve-shadcn-ui-bilesenleri/08-01-SUMMARY.md` documenting the final token values committed.
</output>
