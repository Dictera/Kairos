---
phase: 08-ui-yenileme-renk-paleti-degisikligi-ve-shadcn-ui-bilesenleri
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/form.tsx
  - components/ui/select.tsx
  - components/ui/textarea.tsx
  - components/ui/checkbox.tsx
  - components/ui/radio-group.tsx
  - components/ui/switch.tsx
  - components/ui/card.tsx
  - components/ui/table.tsx
  - components/ui/badge.tsx
  - components/ui/tabs.tsx
  - components/ui/pagination.tsx
  - components/ui/scroll-area.tsx
  - components/ui/avatar.tsx
  - components/ui/dialog.tsx
  - components/ui/alert-dialog.tsx
  - components/ui/popover.tsx
  - components/ui/sonner.tsx
  - components/ui/dropdown-menu.tsx
  - components/ui/calendar.tsx
  - components/ui/command.tsx
  - components/ui/progress.tsx
  - components/ui/breadcrumb.tsx
  - components/ui/collapsible.tsx
  - package.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every shadcn component in D-08 (form, select, textarea, checkbox, radio-group, switch, card, table, badge, tabs, pagination, scroll-area, avatar, dialog, alert-dialog, popover, sonner, dropdown-menu, calendar, command, progress, breadcrumb, collapsible) is installed under components/ui/"
    - "Existing 8 components (button, input, label, separator, sheet, sidebar, skeleton, tooltip) are left untouched"
    - "All components import from @base-ui/react (shadcn v4), not Radix UI"
  artifacts:
    - path: "components/ui/card.tsx"
      provides: "shadcn Card + CardHeader + CardContent primitives"
      exports: ["Card", "CardHeader", "CardContent", "CardFooter", "CardTitle", "CardDescription"]
    - path: "components/ui/form.tsx"
      provides: "react-hook-form integrated Form primitives"
      exports: ["Form", "FormField", "FormItem", "FormLabel", "FormControl", "FormMessage"]
    - path: "components/ui/sonner.tsx"
      provides: "Toaster component for toast notifications"
      exports: ["Toaster"]
  key_links:
    - from: "components/ui/*.tsx (new installs)"
      to: "app/globals.css theme tokens"
      via: "className with bg-primary, text-foreground, etc. referencing Tailwind v4 @theme"
      pattern: "bg-primary|text-primary|bg-background"
---

<objective>
Install the 23 shadcn/ui components listed in D-08 that Phase 2-7 will consume for CRUD forms,
list/detail pages, modals, toasts, calendar, and dashboards. Do NOT touch the 8 components already
installed (button, input, label, separator, sheet, sidebar, skeleton, tooltip).

Purpose: Front-load the entire component surface so later phases never block on "install shadcn X"
mid-feature. Every downstream feature plan can assume the component set is ready.

Output: 23 new files under `components/ui/` and corresponding dependencies added to `package.json`.
</objective>

<execution_context>
@D:/sigorta-takip/.claude/get-shit-done/workflows/execute-plan.md
@D:/sigorta-takip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-yenileme-renk-paleti-degisikligi-ve-shadcn-ui-bilesenleri/08-CONTEXT.md

@components.json
@package.json

<existing_components>
## Already installed under components/ui/ (must NOT be reinstalled)

- button.tsx
- input.tsx
- label.tsx
- separator.tsx
- sheet.tsx
- sidebar.tsx
- skeleton.tsx
- tooltip.tsx

shadcn CLI config (components.json):
- style: "base-nova"
- rsc: true
- tsx: true
- iconLibrary: "lucide"
- aliases: @/components, @/lib/utils, @/components/ui, @/lib, @/hooks

Phase 01 decision: shadcn@latest resolves to v4; v4 uses @base-ui/react (already in package.json) NOT Radix UI.
</existing_components>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install form + data display + navigation shadcn components</name>
  <files>
    components/ui/form.tsx, components/ui/select.tsx, components/ui/textarea.tsx,
    components/ui/checkbox.tsx, components/ui/radio-group.tsx, components/ui/switch.tsx,
    components/ui/card.tsx, components/ui/table.tsx, components/ui/badge.tsx,
    components/ui/tabs.tsx, components/ui/pagination.tsx, components/ui/scroll-area.tsx,
    components/ui/avatar.tsx, package.json
  </files>
  <read_first>
    - components.json (to see shadcn CLI configuration: style "base-nova", iconLibrary "lucide")
    - package.json (to see current deps — @base-ui/react, lucide-react already present)
    - components/ui/button.tsx (reference for existing shadcn v4 component shape)
    - .planning/phases/08-ui-yenileme-renk-paleti-degisikligi-ve-shadcn-ui-bilesenleri/08-CONTEXT.md (D-08 full component list)
  </read_first>
  <action>
Install 13 shadcn/ui components via the shadcn CLI. Use a single batched command so npm runs once.

Run exactly:

```
npx shadcn@latest add form select textarea checkbox radio-group switch card table badge tabs pagination scroll-area avatar --yes --overwrite
```

Rules:
- Use `--yes` to skip confirmation prompts.
- Use `--overwrite` is safe here because NONE of these 13 components currently exist under `components/ui/`.
- If CLI asks any interactive question that `--yes` does not cover, fail the task rather than guess.
- Do NOT run the install inside `components/ui/` — run from project root `D:/sigorta-takip` so shadcn reads `components.json` correctly.
- Do NOT touch `components/ui/button.tsx`, `components/ui/input.tsx`, `components/ui/label.tsx`, `components/ui/separator.tsx`, `components/ui/sheet.tsx`, `components/ui/sidebar.tsx`, `components/ui/skeleton.tsx`, `components/ui/tooltip.tsx`. If the CLI tries to modify any of these, abort the task and report.

After the command completes:
1. Verify each of the 13 files exists under `components/ui/`.
2. Verify `package.json` was updated with any new runtime deps the CLI added (e.g. `@base-ui/react` subpackages, `react-hook-form`, `@hookform/resolvers`, `@tanstack/react-table` if CLI installs it for `table`). Do NOT edit package.json by hand — let the CLI do it.
3. Run `npm install` one more time to guarantee lockfile consistency if shadcn CLI does not auto-install.
  </action>
  <verify>
    <automated>ls components/ui/form.tsx components/ui/select.tsx components/ui/textarea.tsx components/ui/checkbox.tsx components/ui/radio-group.tsx components/ui/switch.tsx components/ui/card.tsx components/ui/table.tsx components/ui/badge.tsx components/ui/tabs.tsx components/ui/pagination.tsx components/ui/scroll-area.tsx components/ui/avatar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `components/ui/form.tsx` exists
    - File `components/ui/select.tsx` exists
    - File `components/ui/textarea.tsx` exists
    - File `components/ui/checkbox.tsx` exists
    - File `components/ui/radio-group.tsx` exists
    - File `components/ui/switch.tsx` exists
    - File `components/ui/card.tsx` exists and contains `export` followed by `Card`
    - File `components/ui/table.tsx` exists and contains `export` followed by `Table`
    - File `components/ui/badge.tsx` exists and contains `export` followed by `Badge`
    - File `components/ui/tabs.tsx` exists and contains `export` followed by `Tabs`
    - File `components/ui/pagination.tsx` exists
    - File `components/ui/scroll-area.tsx` exists
    - File `components/ui/avatar.tsx` exists
    - File `components/ui/button.tsx` is UNCHANGED from before the task (git diff shows no changes)
    - File `components/ui/sidebar.tsx` is UNCHANGED from before the task
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>13 shadcn components added; pre-existing 8 components untouched; TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Install modal + notification + advanced shadcn components</name>
  <files>
    components/ui/dialog.tsx, components/ui/alert-dialog.tsx, components/ui/popover.tsx,
    components/ui/sonner.tsx, components/ui/dropdown-menu.tsx, components/ui/calendar.tsx,
    components/ui/command.tsx, components/ui/progress.tsx, components/ui/breadcrumb.tsx,
    components/ui/collapsible.tsx, package.json
  </files>
  <read_first>
    - components.json
    - package.json (to see deps added by Task 1)
    - components/ui/button.tsx (reference shape)
  </read_first>
  <action>
Install the remaining 10 shadcn/ui components in a single batched command.

Run exactly:

```
npx shadcn@latest add dialog alert-dialog popover sonner dropdown-menu calendar command progress breadcrumb collapsible --yes --overwrite
```

Rules:
- Run from project root `D:/sigorta-takip`.
- The `sonner` component pulls in the `sonner` npm package — let the CLI install it.
- The `calendar` component pulls in `react-day-picker` and `date-fns` — let the CLI install them.
- The `command` component pulls in `cmdk` — let the CLI install it.
- Do NOT touch any of the 8 pre-existing components listed in Task 1's guardrails.
- Do NOT touch any of the 13 Task 1 components either.
- If any install fails (e.g. peer dependency conflict with Tailwind v4), DO NOT force-install; stop and report the exact error.

After the command completes:
1. Verify each of the 10 files exists under `components/ui/`.
2. Run `npm install` to ensure lockfile is consistent.
3. Run `npx tsc --noEmit` — must exit 0.
4. Run `npm run lint` — must exit 0.
  </action>
  <verify>
    <automated>ls components/ui/dialog.tsx components/ui/alert-dialog.tsx components/ui/popover.tsx components/ui/sonner.tsx components/ui/dropdown-menu.tsx components/ui/calendar.tsx components/ui/command.tsx components/ui/progress.tsx components/ui/breadcrumb.tsx components/ui/collapsible.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `components/ui/dialog.tsx` exists and contains `export` followed by `Dialog`
    - File `components/ui/alert-dialog.tsx` exists and contains `export` followed by `AlertDialog`
    - File `components/ui/popover.tsx` exists
    - File `components/ui/sonner.tsx` exists and contains `export` followed by `Toaster`
    - File `components/ui/dropdown-menu.tsx` exists
    - File `components/ui/calendar.tsx` exists
    - File `components/ui/command.tsx` exists
    - File `components/ui/progress.tsx` exists
    - File `components/ui/breadcrumb.tsx` exists
    - File `components/ui/collapsible.tsx` exists
    - `package.json` contains `"sonner"` dependency entry
    - `package.json` contains `"react-day-picker"` dependency entry
    - `package.json` contains `"date-fns"` dependency entry
    - `package.json` contains `"cmdk"` dependency entry
    - Directory `components/ui/` contains exactly 31 `.tsx` files (8 pre-existing + 13 from Task 1 + 10 from Task 2)
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>All 23 D-08 components installed; component surface ready for Phase 2-7 consumption; type check and lint clean.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `ls components/ui/ | wc -l` → 31
2. `npx tsc --noEmit` exits 0
3. `npm run lint` exits 0
4. `git diff components/ui/button.tsx components/ui/input.tsx components/ui/label.tsx components/ui/separator.tsx components/ui/sheet.tsx components/ui/sidebar.tsx components/ui/skeleton.tsx components/ui/tooltip.tsx` is empty (pre-existing components untouched)
</verification>

<success_criteria>
1. All 23 shadcn components from D-08 installed under `components/ui/`
2. Pre-existing 8 components are byte-identical to their state before this plan (git diff empty)
3. `package.json` contains CLI-added runtime deps (sonner, react-day-picker, date-fns, cmdk at minimum)
4. TypeScript strict check passes; ESLint passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-yenileme-renk-paleti-degisikligi-ve-shadcn-ui-bilesenleri/08-02-SUMMARY.md` listing each component installed, its export surface, and any new package.json dependencies added.
</output>
