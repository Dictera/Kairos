---
phase: 09-projedeki-b-t-n-takvim-g-r-n-mlerinin-dosyalar-dosya-detay-y
plan: '02'
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - components/dosya/stk-data-form.tsx
  - components/dosya/mahkeme-data-form.tsx
  - components/dosya/durusma-dialog.tsx
  - components/dosya/dosya-list.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Reference forms import DatePickerField from shared component"
    - "Inline DatePickerField definitions removed from reference forms"
    - "DosyaList filter inputs use DatePickerField with Turkish locale"
    - "All DatePickerField usages share consistent weekStartsOn={1} and locale=tr"
  artifacts:
    - path: "components/dosya/stk-data-form.tsx"
      provides: "Uses shared DatePickerField import"
      contains: "from '@/components/ui/date-picker'"
    - path: "components/dosya/mahkeme-data-form.tsx"
      provides: "Uses shared DatePickerField import"
      contains: "from '@/components/ui/date-picker'"
    - path: "components/dosya/durusma-dialog.tsx"
      provides: "Uses shared DatePickerField import"
      contains: "from '@/components/ui/date-picker'"
    - path: "components/dosya/dosya-list.tsx"
      provides: "Date filter inputs use DatePickerField"
      contains: "DatePickerField"
  key_links:
    - from: "components/dosya/stk-data-form.tsx"
      to: "components/ui/date-picker.tsx"
      via: "import statement"
      pattern: "import.*DatePickerField.*from"
    - from: "components/dosya/dosya-list.tsx"
      to: "components/ui/date-picker.tsx"
      via: "import statement"
      pattern: "import.*DatePickerField.*from"
---

<objective>
Update reference forms (stk-data-form, mahkeme-data-form, durusma-dialog) to import DatePickerField from the shared component, and update dosya-list.tsx filter inputs to use DatePickerField.

Purpose: Complete the DatePickerField extraction, removing all inline definitions. Ensure dosya-list filter inputs have consistent Turkish locale and Monday-first week.

Output: All reference forms use shared DatePickerField, dosya-list filter inputs updated.
</objective>

<context>
@.planning/phases/09-projedeki-b-t-n-takvim-g-r-n-mlerinin-dosyalar-dosya-detay-y/09-CONTEXT.md
@.planning/phases/09-projedeki-b-t-n-takvim-g-r-n-mlerinin-dosyalar-dosya-detay-y/09-RESEARCH.md
@.planning/phases/09-projedeki-b-t-n-takvim-g-r-n-mlerinin-dosyalar-dosya-detay-y/09-01-PLAN.md
@components/ui/date-picker.tsx (shared DatePickerField created in Plan 01)
@components/dosya/stk-data-form.tsx (DatePickerField at lines 45-78 to remove)
@components/dosya/mahkeme-data-form.tsx (DatePickerField at lines 45-78 to remove)
@components/dosya/durusma-dialog.tsx (DatePickerField at lines 49-82 to remove)
@components/dosya/dosya-list.tsx (lines 175-191: Input type="date" filter inputs to replace)
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From components/dosya/dosya-list.tsx (state management for filters):
```tsx
const [tarihBaslangic, setTarihBaslangic] = useState('')
const [tarihBitis, setTarihBitis] = useState('')

// In onChange handler:
onChange={(e) => { setTarihBaslangic(e.target.value); setPage(1) }}
```

DatePickerField interface:
```tsx
interface DatePickerFieldProps {
  value: string | null | undefined
  onChange: (value: string | undefined) => void
  placeholder?: string
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update stk-data-form.tsx to use shared DatePickerField</name>
  <files>components/dosya/stk-data-form.tsx</files>
  <read_first>
    - components/dosya/stk-data-form.tsx (lines 1-30: imports, lines 45-78: inline DatePickerField)
    - components/ui/date-picker.tsx (shared component)
  </read_first>
  <behavior>
    - stk-data-form.tsx imports DatePickerField from shared location
    - Inline DatePickerField function is removed
    - All DatePickerField usages in form (basvuru_tarihi, kabul_tarihi, hakem_karar_tarihi, tebligat_tarihi, itiraz_tarihi) continue to work via the import
  </behavior>
  <action>
In `components/dosya/stk-data-form.tsx`:

1. Add import for DatePickerField (around line 11, after CalendarIcon import):
```tsx
import { DatePickerField } from '@/components/ui/date-picker'
```

2. Remove the inline DatePickerField function definition (lines 45-78)

3. Keep the rest of the file unchanged - the DatePickerField usages in the form will now use the imported version.

The form fields using DatePickerField (basvuru_tarihi, kabul_tarihi, hakem_karar_tarihi, tebligat_tarihi, itiraz_tarihi) will automatically use the imported component since they already call DatePickerField with the same props interface.
  </action>
  <verify>
    <automated>grep -c "import.*DatePickerField.*from.*date-picker" components/dosya/stk-data-form.tsx</automated>
  </verify>
  <done>
    stk-data-form.tsx imports DatePickerField from shared component, inline definition removed
  </done>
</task>

<task type="auto">
  <name>Task 2: Update mahkeme-data-form.tsx to use shared DatePickerField</name>
  <files>components/dosya/mahkeme-data-form.tsx</files>
  <read_first>
    - components/dosya/mahkeme-data-form.tsx (lines 1-30: imports, lines 45-78: inline DatePickerField)
    - components/ui/date-picker.tsx (shared component)
  </read_first>
  <behavior>
    - mahkeme-data-form.tsx imports DatePickerField from shared location
    - Inline DatePickerField function is removed
    - Form fields continue to use DatePickerField via the import
  </behavior>
  <action>
In `components/dosya/mahkeme-data-form.tsx`:

1. Add import for DatePickerField (after CalendarIcon import):
```tsx
import { DatePickerField } from '@/components/ui/date-picker'
```

2. Remove the inline DatePickerField function definition (lines 45-78)

3. Keep the rest of the file unchanged.
  </action>
  <verify>
    <automated>grep -c "import.*DatePickerField.*from.*date-picker" components/dosya/mahkeme-data-form.tsx</automated>
  </verify>
  <done>
    mahkeme-data-form.tsx imports DatePickerField from shared component, inline definition removed
  </done>
</task>

<task type="auto">
  <name>Task 3: Update durusma-dialog.tsx to use shared DatePickerField</name>
  <files>components/dosya/durusma-dialog.tsx</files>
  <read_first>
    - components/dosya/durusma-dialog.tsx (lines 1-30: imports, lines 49-82: inline DatePickerField)
    - components/ui/date-picker.tsx (shared component)
  </read_first>
  <behavior>
    - durusma-dialog.tsx imports DatePickerField from shared location
    - Inline DatePickerField function is removed
    - Form field (tarih) continues to use DatePickerField via the import
  </behavior>
  <action>
In `components/dosya/durusma-dialog.tsx`:

1. Add import for DatePickerField (after CalendarIcon import):
```tsx
import { DatePickerField } from '@/components/ui/date-picker'
```

2. Remove the inline DatePickerField function definition (lines 49-82)

3. Keep the rest of the file unchanged.
  </action>
  <verify>
    <automated>grep -c "import.*DatePickerField.*from.*date-picker" components/dosya/durusma-dialog.tsx</automated>
  </verify>
  <done>
    durusma-dialog.tsx imports DatePickerField from shared component, inline definition removed
  </done>
</task>

<task type="auto">
  <name>Task 4: Update dosya-list.tsx filter date inputs</name>
  <files>components/dosya/dosya-list.tsx</files>
  <read_first>
    - components/dosya/dosya-list.tsx (lines 35-45: imports and state, lines 173-191: date filter inputs)
    - components/ui/date-picker.tsx (shared DatePickerField component)
  </read_first>
  <behavior>
    - dosya-list.tsx filter section uses DatePickerField for tarihBaslangic and tarihBitis
    - DatePickerField provides Turkish locale (dd.MM.yyyy) and Monday-first calendar
    - onChange handler properly updates state and resets pagination
  </behavior>
  <action>
In `components/dosya/dosya-list.tsx`:

1. Add import at top (with other imports):
```tsx
import { DatePickerField } from '@/components/ui/date-picker'
```

2. Replace the Başlangıç filter (around lines 173-181):
```tsx
<div className="space-y-1">
  <label className="text-sm font-semibold text-muted-foreground">Başlangıç</label>
  <DatePickerField
    value={tarihBaslangic}
    onChange={(val) => { setTarihBaslangic(val || ''); setPage(1) }}
    placeholder="Başlangıç tarihi"
  />
</div>
```

3. Replace the Bitiş filter (around lines 183-191):
```tsx
<div className="space-y-1">
  <label className="text-sm font-semibold text-muted-foreground">Bitiş</label>
  <DatePickerField
    value={tarihBitis}
    onChange={(val) => { setTarihBitis(val || ''); setPage(1) }}
    placeholder="Bitiş tarihi"
  />
</div>
```

Remove the old `<Input type="date" className="w-[160px]" ... />` lines.
  </action>
  <verify>
    <automated>grep -c "DatePickerField" components/dosya/dosya-list.tsx</automated>
  </verify>
  <done>
    dosya-list.tsx filter inputs use DatePickerField with Turkish locale and Monday week start
  </done>
</task>

</tasks>

<verification>
1. Verify stk-data-form.tsx imports from date-picker: `grep -l "import.*DatePickerField.*from.*date-picker" components/dosya/stk-data-form.tsx`
2. Verify mahkeme-data-form.tsx imports from date-picker: `grep -l "import.*DatePickerField.*from.*date-picker" components/dosya/mahkeme-data-form.tsx`
3. Verify durusma-dialog.tsx imports from date-picker: `grep -l "import.*DatePickerField.*from.*date-picker" components/dosya/durusma-dialog.tsx`
4. Verify dosya-list.tsx uses DatePickerField: `grep -c "DatePickerField" components/dosya/dosya-list.tsx`
5. Run type check: `npm run typecheck` or `npx tsc --noEmit`
</verification>

<success_criteria>
- stk-data-form.tsx imports DatePickerField, inline definition removed
- mahkeme-data-form.tsx imports DatePickerField, inline definition removed
- durusma-dialog.tsx imports DatePickerField, inline definition removed
- dosya-list.tsx filter inputs use DatePickerField
- All date pickers display in dd.MM.yyyy Turkish format with Monday-first week
</success_criteria>

<output>
After completion, create `.planning/phases/09-projedeki-b-t-n-takvim-g-r-n-mlerinin-dosyalar-dosya-detay-y/09-02-SUMMARY.md`
</output>
