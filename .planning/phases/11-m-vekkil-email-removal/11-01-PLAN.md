---
phase: 11-m-vekkil-email-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drizzle/meta/_journal.json
  - drizzle/meta/0000_snapshot.json
  - drizzle/0000_*.sql
autonomous: true
requirements:
  - MUVEK-05
must_haves:
  truths:
    - "drizzle-kit generate produces a clean single migration matching current schema"
    - "drizzle/meta/_journal.json has exactly 1 entry referencing 0000_snapshot.json"
    - "Generated migration SQL has no email column in muvekkil table"
    - "All existing tests pass after regeneration"
  artifacts:
    - path: "drizzle/meta/_journal.json"
      provides: "Single journal entry for current schema state"
      contains: "tag"
    - path: "drizzle/meta/0000_snapshot.json"
      provides: "Snapshot of complete current schema"
    - path: "drizzle/0000_*.sql"
      provides: "Single initial migration with all CREATE TABLE statements"
      contains: "muvekkil"
  key_links:
    - from: "drizzle/0000_*.sql"
      to: "lib/schema.ts"
      via: "drizzle-kit generate reads schema.ts to produce migration"
      pattern: "CREATE TABLE.*muvekkil"
---

<objective>
Regenerate Drizzle migration metadata to resolve the inconsistency between journal entries (5) and SQL files (7), producing a clean single migration that matches the current schema state with no email column in muvekkil.

Purpose: The current drizzle metadata is inconsistent — 5 journal entries for 7 SQL files, with entry #4 having the wrong tag. Drizzle-kit cannot fix this incrementally; nuclear regeneration (delete + regenerate) is the correct approach. Resolves D-01 (regenerate metadata) while honoring D-02's intent (migration history preserved in git).

Output: Clean drizzle/ directory with single initial migration matching current schema.
</objective>

<execution_context>
@D:/sigorta-takip/.opencode/get-shit-done/workflows/execute-plan.md
@D:/sigorta-takip/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-m-vekkil-email-removal/11-CONTEXT.md
@.planning/phases/11-m-vekkil-email-removal/11-RESEARCH.md
@.planning/phases/11-m-vekkil-email-removal/11-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/schema.ts (line 123-133):
```typescript
export const muvekkil = sqliteTable('muvekkil', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  ad: text('ad').notNull(),
  soyad: text('soyad').notNull(),
  telefon: text('telefon'),
  tc_vergi_no: text('tc_vergi_no'),
  adres: text('adres'),
  notlar: text('notlan'),
  created_at: text('created_at').notNull().default(sql`(datetime('now'))`),
  updated_at: text('updated_at').notNull().default(sql`(datetime('now'))`),
})
// NO email column — this is the correct current state

From drizzle.config.ts:
```typescript
export default defineConfig({
  dialect: 'sqlite',
  schema: './lib/schema.ts',
  out: './drizzle',
  dbCredentials: { url: './data/db.sqlite' },
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Regenerate drizzle metadata with nuclear approach</name>
  <files>drizzle/meta/_journal.json, drizzle/meta/0000_snapshot.json, drizzle/0000_*.sql</files>
  <read_first>
    - lib/schema.ts (current muvekkil table schema — NO email column)
    - drizzle.config.ts (drizzle config pointing to schema and out directory)
    - drizzle/meta/_journal.json (current inconsistent journal — 5 entries, wrong tag on idx 4)
    - .planning/phases/11-m-vekkil-email-removal/11-CONTEXT.md (D-01: regenerate metadata, D-02: keep SQL files in git history)
    - .planning/phases/11-m-vekkil-email-removal/11-RESEARCH.md (Pitfall 1: nuclear approach required, Pitfall 4: don't run migrate after regeneration)
  </read_first>
  <acceptance_criteria>
    - drizzle/meta/_journal.json contains exactly 1 entry with idx 0
    - drizzle/meta/0000_snapshot.json exists and is valid JSON
    - drizzle/ directory contains exactly 1 .sql file (named 0000_*.sql)
    - Generated SQL file contains "CREATE TABLE" and "muvekkil" but does NOT contain "email" in the muvekkil table definition
    - Generated SQL file contains "surucu_ad", "surucu_soyad", "surucu_plaka", "surucu_telefon", "surucu_police_no" (driver fields)
    - `npx vitest run` exits with code 0
    - Old files 0001-0006 .sql files no longer exist in drizzle/
    - Old meta snapshots 0001-0004 no longer exist (only 0000_snapshot.json remains)
  </acceptance_criteria>
  <behavior>
    - Before: drizzle/meta/ has 5 snapshots for 7 SQL files, journal has wrong tag on idx 4
    - After deletion: drizzle/meta/ and drizzle/*.sql are empty/removed
    - After regeneration: drizzle-kit generate creates single 0000 migration + snapshot + journal with 1 entry
    - Generated SQL contains muvekkil table WITHOUT email column
    - Generated SQL contains taraf table WITH surucu_* columns
    - All vitest tests pass after regeneration
  </behavior>
  <action>
**Resolve D-01 vs D-02 conflict:** D-01 (regenerate) takes priority. D-02's intent (preserve migration history) is satisfied by git history — the 7 SQL files are already committed and preserved there. Nuclear regeneration is the correct approach per RESEARCH.md.

**Step 1: Commit current state as checkpoint**
Run: `git add -A && git commit -m "chore: checkpoint before drizzle metadata regeneration"`

**Step 2: Delete all existing drizzle migration artifacts**
```powershell
Remove-Item -Recurse -Force drizzle/meta/*
Remove-Item -Force drizzle/*.sql
```

**Step 3: Regenerate from current schema**
Run: `npx drizzle-kit generate`
This reads `lib/schema.ts` via `drizzle.config.ts` and produces:
- `drizzle/0000_<random_name>.sql` — single migration with all CREATE TABLE statements
- `drizzle/meta/0000_snapshot.json` — snapshot of current schema
- `drizzle/meta/_journal.json` — single entry referencing the snapshot

**Step 4: Verify generated migration**
Check the generated SQL file:
- Must contain `CREATE TABLE` for `muvekkil` WITHOUT an `email` column
- Must contain `CREATE TABLE` for `taraf` WITH columns: `surucu_ad`, `surucu_soyad`, `surucu_plaka`, `surucu_telefon`, `surucu_police_no`
- Must contain `CREATE TABLE` for `dilekce_sablonu`
- Must contain `CREATE TABLE` for `dilekce_odt_sablonu`
- Must NOT contain `email` anywhere related to muvekkil

Check the journal:
- `_journal.json` must have exactly 1 entry with `idx: 0`
- Entry must reference `0000_snapshot.json`
- No trace of `0004_drop_muvekkil_email` or other old migration tags

**Step 5: Verify tests still pass**
Run: `npx vitest run`

**Step 6: Commit regenerated metadata**
Run: `git add -A && git commit -m "chore(11): regenerate drizzle metadata — single clean migration"`

**IMPORTANT:** Do NOT run `drizzle-kit migrate` or `drizzle-kit push` — the SQLite database is already in the correct state (migrated by Phase 10). The `__drizzle_migrations` tracking table may have stale entries. The regeneration is purely metadata cleanup; the DB doesn't need re-migration.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>
    - drizzle/meta/_journal.json has exactly 1 entry with idx 0
    - drizzle/meta/0000_snapshot.json exists and reflects current schema
    - drizzle/0000_*.sql exists as single migration file
    - Generated SQL has muvekkil table WITHOUT email column
    - Generated SQL has taraf table WITH surucu_* columns (5 driver fields)
    - All vitest tests pass
    - Old inconsistent migration files (0001-0006) are removed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase involves no security-sensitive changes — only metadata cleanup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | drizzle metadata | accept | Local development only — no external access to drizzle files; git tracks all changes |
| T-11-02 | Info Disclosure | migration SQL | accept | Migration SQL is schema definition, not sensitive data |

No security-sensitive changes in this phase. Metadata regeneration is a development tooling operation with no authentication, authorization, or input validation surface.
</threat_model>

<verification>
- `npx vitest run` passes all tests
- `drizzle/meta/_journal.json` has exactly 1 entry
- `drizzle/0000_*.sql` contains muvekkil table description without email
- `git status` shows clean working directory after commit
</verification>

<success_criteria>
- Drizzle metadata is consistent: 1 journal entry, 1 snapshot, 1 SQL file
- Generated migration reflects current schema (muvekkil without email, taraf with driver fields)
- All existing tests pass
- Old inconsistent metadata files removed
</success_criteria>

<output>
After completion, create `.planning/phases/11-m-vekkil-email-removal/11-01-SUMMARY.md`
</output>