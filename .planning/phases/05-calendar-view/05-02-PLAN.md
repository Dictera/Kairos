---
phase: "05"
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - app/takvim/page.tsx
  - components/calendar/calendar-day-cell.tsx
  - components/calendar/calendar-event-popover.tsx
  - components/calendar/calendar-view.tsx
autonomous: true
requirements:
  - TAKVIM-01
  - TAKVIM-02
user_setup: []

must_haves:
  truths:
    - "Calendar displays a full monthly grid with event markers per day"
    - "Clicking a day with events shows a popover with clickable event list"
    - "Clicking an event in the popover navigates to /dosyalar/{dosya_id}"
    - "User can navigate between months with prev/next buttons and dropdown"
  artifacts:
    - path: app/takvim/page.tsx
      provides: Calendar page route
      exports: TakvimPage component
    - path: components/calendar/calendar-day-cell.tsx
      provides: Custom day button with badge counts
      exports: CalendarDayCell
    - path: components/calendar/calendar-event-popover.tsx
      provides: Event list popover content
      exports: CalendarEventPopover
    - path: components/calendar/calendar-view.tsx
      provides: Main calendar component with data integration
      exports: CalendarView
  key_links:
    - from: app/takvim/page.tsx
      to: components/calendar/calendar-view.tsx
      via: imports and renders
      pattern: CalendarView
    - from: components/calendar/calendar-view.tsx
      to: trpc.calendar.getMonthEvents
      via: useQuery
      pattern: trpc.calendar.getMonthEvents.useQuery
    - from: components/calendar/calendar-event-popover.tsx
      to: /dosyalar/[id]
      via: Next.js Link
      pattern: Link.*href=.*dosyalar
---

<objective>
Build the calendar view UI — a monthly grid with event markers per day, an event popover on day click, and month navigation. This plan creates the frontend components and page, consuming the tRPC procedure from Plan 01.
</objective>

<execution_context>
@D:/sigorta-takip/.opencode/get-shit-done/workflows/execute-plan.md
@D:/sigorta-takip/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-calendar-view/05-CONTEXT.md
@.planning/phases/05-calendar-view/05-RESEARCH.md
@.planning/phases/05-calendar-view/05-UI-SPEC.md
@components/ui/calendar.tsx
@components/ui/card.tsx
@components/ui/popover.tsx
@components/ui/badge.tsx
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From 05-RESEARCH.md (calendar router response type):
```typescript
type MonthEvent = {
  id: number
  type: 'süre' | 'duruşma'
  ad: string
  dosya_id: number
  dosya_no: string
  muvekkil_ad: string
  tarih: string  // YYYY-MM-DD
  saat: string | null  // HH:mm or null for süre
}
```

From components/ui/calendar.tsx (Calendar component props):
```typescript
<Calendar
  month={month}
  onMonthChange={setMonth}
  locale={tr}
  startWeekOn={1}
  captionLayout="dropdown"
  components={{
    DayButton: (props) => <CalendarDayButton {...props} locale={tr} />
  }}
/>
```

From 05-UI-SPEC.md (event badge styling):
```typescript
// D-01: Inline badge format per day cell
<span className="text-[9px] text-red-500 font-medium">
  {events.sureCount} süre
</span>
<span className="text-[9px] text-blue-500 font-medium">
  {events.durusmaCount} duruşma
</span>

// D-03: Popover item format
[badge] {event.ad}
{muvekkil_ad} — #{dosya_no}[ if duruşma: • saat]
```

From 05-CONTEXT.md (Turkish locale):
```typescript
import { tr } from 'date-fns/locale/tr'
// dd.MM.yyyy date format, Pazartesi week start
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create CalendarDayCell component with inline badge counts</name>
  <files>components/calendar/calendar-day-cell.tsx</files>
  <read_first>
    - components/ui/calendar.tsx (CalendarDayButton pattern)
    - components/ui/button.tsx
    - .planning/phases/05-calendar-view/05-UI-SPEC.md (D-01 styling)
  </read_first>
  <action>
    Create components/calendar/calendar-day-cell.tsx:

1. **Imports:**
   - DayButton, type DayButtonProps from react-day-picker
   - Button from @/components/ui/button
   - cn from @/lib/utils
   - type Locale from react-day-picker

2. **Props type:**
   ```typescript
   type CalendarDayCellProps = DayButtonProps & {
     locale?: Partial<Locale>
     events: {
       sureCount: number
       durusmaCount: number
     }
   }
   ```

3. **Component:**
   - Use Button with variant="ghost", size="icon"
   - Render day number: {day.date.getDate()}
   - Below day number, render two lines if counts > 0:
     - Line 1: "{sureCount} süre" in text-red-500, text-[9px], font-medium, leading-none
     - Line 2: "{durusmaCount} duruşma" in text-blue-500, text-[9px], font-medium, leading-none
   - Only render lines if count > 0
   - Apply flex flex-col items-center justify-center gap-0.5 to container
   - Pass all props through to Button

4. **Styling per D-01 and UI-SPEC:**
   - Container: relative flex flex-col items-center justify-center gap-0.5 h-full w-full
   - Day number: default text
   - Badge text: text-[9px] leading-none
   - Süre: text-red-500 font-medium
   - Duruşma: text-blue-500 font-medium
  </action>
  <verify>
    <automated>grep -l "CalendarDayCell" components/calendar/calendar-day-cell.tsx && grep "sureCount" components/calendar/calendar-day-cell.tsx</automated>
  </verify>
  <done>CalendarDayCell renders inline badge counts per day cell in correct format and colors</done>
</task>

<task type="auto">
  <name>Task 2: Create CalendarEventPopover component</name>
  <files>components/calendar/calendar-event-popover.tsx</files>
  <read_first>
    - components/ui/popover.tsx
    - components/ui/badge.tsx
    - .planning/phases/05-calendar-view/05-UI-SPEC.md (D-03 popover content)
  </read_first>
  <action>
    Create components/calendar/calendar-event-popover.tsx:

1. **Imports:**
   - PopoverContent, PopoverHeader, PopoverTitle, PopoverDescription from @/components/ui/popover
   - Badge from @/components/ui/badge
   - format from date-fns
   - tr from date-fns/locale/tr
   - Link from next/link

2. **Type:**
   ```typescript
   type CalendarEvent = {
     id: number
     type: 'süre' | 'duruşma'
     ad: string
     dosya_id: number
     dosya_no: string
     muvekkil_ad: string
     tarih: string
     saat: string | null
   }
   
   type CalendarEventPopoverProps = {
     events: CalendarEvent[]
     selectedDate: Date
   }
   ```

3. **Component:**
   - Format selectedDate: format(selectedDate, 'dd.MM.yyyy', { locale: tr })
   - Show event count: "{events.length} etkinlik" in description
   - Sort events: süre first, then duruşma, chronological within type (per D-04)
   - Render each event as a clickable Link to /dosyalar/${event.dosya_id}
   - Event item structure (per D-03):
     - Badge: variant={event.type === 'süre' ? 'destructive' : 'secondary'}
     - Event name: text-sm font-medium truncate
     - Details: "muvekkil_ad — #dosya_no" in text-xs text-muted-foreground
     - If duruşma: add " • saat" after dosya_no
   - Wrap items in scrollable container if many events
  </action>
  <verify>
    <automated>grep -l "CalendarEventPopover" components/calendar/calendar-event-popover.tsx && grep "Link.*dosyalar" components/calendar/calendar-event-popover.tsx</automated>
  </verify>
  <done>CalendarEventPopover displays events sorted by type with clickable links to case detail pages</done>
</task>

<task type="auto">
  <name>Task 3: Create CalendarView main component with data integration</name>
  <files>components/calendar/calendar-view.tsx</files>
  <read_first>
    - components/ui/calendar.tsx
    - lib/trpc/client.ts (tRPC client pattern)
    - .planning/phases/05-calendar-view/05-RESEARCH.md (Pattern 1: Controlled Month)
  </read_first>
  <action>
    Create components/calendar/calendar-view.tsx:

1. **Imports:**
   - Calendar from @/components/ui/calendar
   - CalendarDayCell from './calendar-day-cell'
   - CalendarEventPopover from './calendar-event-popover'
   - Popover, PopoverTrigger from @/components/ui/popover
   - useState from react
   - tr from date-fns/locale/tr
   - useQuery from @tanstack/react-query (or useTRPC hook)

2. **State:**
   - month: useState<Date>(new Date())
   - selectedDay: useState<Date | null>(null)
   - popoverOpen: useState(false)

3. **Data fetching:**
   - useQuery with trpc.calendar.getMonthEvents.queryOptions({
       year: month.getFullYear(),
       month: month.getMonth() + 1
     })
   - React Query key: ['calendar', 'monthEvents', year, month]
   - Use keepPreviousData for smooth transitions (per D-06)

4. **getEventsForDate helper:**
   - Filter data.events by tarih === format(day, 'yyyy-MM-dd')
   - Return { sureCount, durusmaCount, events: CalendarEvent[] }

5. **handleDayClick:**
   - Get events for clicked day
   - If events.length === 0: return early (D-05: silent ignore)
   - Set selectedDay(day)
   - Set popoverOpen(true)

6. **Render:**
   - Wrap Calendar in Popover
   - PopoverTrigger wraps the calendar area (or use div)
   - PopoverContent shows CalendarEventPopover when open
   - Calendar with: locale={tr}, startWeekOn={1}, month={month}, onMonthChange={setMonth}
   - DayButton component override: (props) => <CalendarDayCell {...props} locale={tr} events={getEventsForDate(props.day.date)} />
  </action>
  <verify>
    <automated>grep -l "CalendarView" components/calendar/calendar-view.tsx && grep "trpc.calendar.getMonthEvents" components/calendar/calendar-view.tsx</automated>
  </verify>
  <done>CalendarView component integrates data fetching, day click handling, and popover display</done>
</task>

<task type="auto">
  <name>Task 4: Create takvim page route</name>
  <files>app/takvim/page.tsx</files>
  <read_first>
    - app/(dashboard)/layout.tsx (layout pattern)
    - components/ui/card.tsx
    - .planning/phases/05-calendar-view/05-CONTEXT.md (page title)
  </read_first>
  <action>
    Create app/takvim/page.tsx:

1. **Imports:**
   - CalendarView from @/components/calendar/calendar-view
   - Card, CardContent, CardHeader, CardTitle from @/components/ui/card

2. **Component:**
   - Page component with "use client" directive
   - Card wrapper with header: "Takvim" title
   - CalendarView component inside CardContent
   - No props needed — CalendarView handles all its own state

3. **Layout per UI-SPEC:**
   - Card with lg-24 padding for section
   - Page-level spacing with xl-32 gap
  </action>
  <verify>
    <automated>grep -l "Takvim" app/takvim/page.tsx && grep "CalendarView" app/takvim/page.tsx</automated>
  </verify>
  <done>Takvim page renders CalendarView in a Card wrapper</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → navigation | Clicking event link navigates to /dosyalar/{id} — authenticated route protected by middleware |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | CalendarView month state | mitigate | Month is derived from Date object, not user input — no injection risk |
| T-05-04 | Information Disclosure | Event popover | accept | Shows case info user already has access to via dashboard |
</threat_model>

<verification>
1. Run: npm test -- --filter="calendar" (if tests exist)
2. Start dev server: next dev
3. Visit: http://localhost:3000/takvim
4. Verify calendar renders with Turkish locale
5. Verify clicking a day with events shows popover
6. Verify clicking event in popover navigates to /dosyalar/{id}
</verification>

<success_criteria>
- Calendar page renders at /takvim with full monthly grid
- Event markers (süre/duruşma counts) appear on days with events
- Clicking day with events shows popover with event list
- Clicking event in popover navigates to correct case detail page
- Prev/next month navigation works
- Month dropdown (captionLayout="dropdown") works for direct month jump
- Turkish locale displays correctly (dd.MM.yyyy, Pazartesi week start)
- Empty days (D-05): clicking does nothing, no popover
</success_criteria>

<output>
After completion, create `.planning/phases/05-calendar-view/05-02-SUMMARY.md`
</output>
