---
phase: 26-bildirim-sisteminin-iyilestirilmesi
plan: "00"
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/26-bildirim.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "tests/26-bildirim.test.ts exists with 8 describe blocks BLD-01 through BLD-08"
    - "pnpm test tests/26-bildirim.test.ts runs without import errors and all tests are RED (failing)"
  artifacts:
    - path: "tests/26-bildirim.test.ts"
      provides: "Phase 26 test scaffold — all 8 BLD requirement stubs"
      min_lines: 80
  key_links:
    - from: "tests/26-bildirim.test.ts"
      to: "lib/telegram/settings-helper"
      via: "dynamic import"
      pattern: "import.*settings-helper"
    - from: "tests/26-bildirim.test.ts"
      to: "lib/trpc/routers/telegram"
      via: "dynamic import"
      pattern: "import.*routers/telegram"
---

<objective>
Wave 0: Create the RED test scaffold for Phase 26. All 8 test cases must fail initially because the implementation files do not yet exist (settings-helper.ts) or have not yet been modified.

Purpose: Establishes the verification gate used at the end of every subsequent wave. Executors in Waves 1–3 run `pnpm test tests/26-bildirim.test.ts` after each change and watch tests turn GREEN.
Output: tests/26-bildirim.test.ts with 8 failing describe blocks.
</objective>

<execution_context>
@D:/sigorta-takip/.claude/get-shit-done/workflows/execute-plan.md
@D:/sigorta-takip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/26-bildirim-sisteminin-iyilestirilmesi/26-CONTEXT.md
@.planning/phases/26-bildirim-sisteminin-iyilestirilmesi/26-RESEARCH.md
@.planning/phases/26-bildirim-sisteminin-iyilestirilmesi/26-PATTERNS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write RED test stubs for BLD-01 through BLD-08</name>
  <files>tests/26-bildirim.test.ts</files>

  <read_first>
    - tests/25-telegram.test.ts — exact analog: file header, import pattern, describe block structure, dynamic import style, beforeEach/afterEach pattern, router procedure existence test using `telegramRouter._def.record`
    - lib/telegram/notify.ts — current exported function name `sendPendingTelegramNotifications` (used in BLD-02/03/04/05 stubs)
    - lib/telegram/weekly.ts — current exported function name `sendWeeklySureSummary` (used in BLD-06/07 stubs)
    - lib/trpc/routers/telegram.ts — current procedure names (BLD-08 checks for getToggles + updateToggles which do NOT yet exist)
  </read_first>

  <behavior>
    BLD-01: Toggle default = true when key absent
      - imports `{ readSettings }` from `@/lib/telegram/settings-helper` (dynamic import)
      - test: readSettings() returns an object where `(result.telegram_gunluk_durusma_aktif as boolean | undefined) ?? true` equals `true`
      - RED because lib/telegram/settings-helper.ts does not exist yet

    BLD-02: Grouped message format — durusma block rendered correctly
      - imports `{ buildGroupedMessage }` from `@/lib/telegram/notify` (dynamic import)
      - test: calling buildGroupedMessage with one durusma row for tomorrow produces a string containing `<b>Yarınki Duruşmalar</b>`
      - RED because buildGroupedMessage is not yet exported from notify.ts

    BLD-03: Grouped message — empty blocks omitted (D-15)
      - imports `{ buildGroupedMessage }` from `@/lib/telegram/notify`
      - test: calling buildGroupedMessage with all empty arrays returns null
      - RED because buildGroupedMessage is not yet exported

    BLD-04: Grouped message — sentIds only includes rows in message
      - imports `{ buildSentIds }` from `@/lib/telegram/notify` (or describe the behavior contract)
      - test: when `telegram_gunluk_durusma_aktif=false`, durusma row ids are excluded from the sentIds result
      - RED because the grouped logic with per-category filtering does not exist yet

    BLD-05: Toggle disabled → rows not included in message
      - imports `{ buildGroupedMessage }` from `@/lib/telegram/notify`
      - test: calling buildGroupedMessage with durusmaAktif=false and only durusma rows returns null (nothing to send)
      - RED because buildGroupedMessage is not yet exported

    BLD-06: weekly.ts durusma rows included in output
      - imports `sendWeeklySureSummary` from `@/lib/telegram/weekly`
      - test: the function is exported (typeof === 'function') — RED test that the function signature accepts durusma join (tested by checking durusma is imported in the module, or checking behavior via a mock)
      - Use existence test as RED gate: import `{ durusma }` from `@/lib/schema` and verify weekly.ts imports it (structural test)

    BLD-07: weekly.ts toggle=false → returns early
      - imports `sendWeeklySureSummary` from `@/lib/telegram/weekly`
      - mocks readSettings to return `{ telegram_haftalik_ozet_aktif: false }`
      - mocks sendTelegramMessage from `@/lib/telegram/send`
      - test: sendTelegramMessage is NOT called when toggle is false
      - RED because the toggle check does not yet exist in weekly.ts

    BLD-08: telegramRouter has getToggles + updateToggles
      - imports `{ telegramRouter }` from `@/lib/trpc/routers/telegram`
      - test: `telegramRouter._def.record` has property `getToggles`
      - test: `telegramRouter._def.record` has property `updateToggles`
      - RED because these procedures do not yet exist
  </behavior>

  <action>
    Create tests/26-bildirim.test.ts with the following structure — do NOT include fenced code blocks or full implementations:

    File header: JSDoc comment with phase name, "Wave 0: All tests start RED", "Requirements: BLD-01 through BLD-08". Import line: `import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'`.

    8 describe blocks in order BLD-01 through BLD-08. Each describe block label matches the behavior description above. Each has at least one `it()` call using dynamic `await import(...)` for the module under test.

    BLD-02, BLD-03, BLD-05: define a helper type `type Row = { id: number; tip: string; tarih: string; dosya_no: string | null; mesaj: string | null }` at the top of the file (after the import) to use in test data.

    BLD-07: use `vi.mock('@/lib/telegram/send', ...)` or `vi.spyOn` pattern matching `25-telegram.test.ts` line 60 style — mock sendTelegramMessage to return `Promise<boolean>`. Assert it was not called via `expect(mockFn).not.toHaveBeenCalled()`.

    BLD-08: use `telegramRouter._def.record` property check, exactly matching `25-telegram.test.ts` router existence pattern.

    Every test must be genuinely RED: imports from `@/lib/telegram/settings-helper` will throw ModuleNotFoundError; `buildGroupedMessage` export does not exist in notify.ts; getToggles/updateToggles procedures do not exist in telegram.ts router.
  </action>

  <verify>
    <automated>cd D:/sigorta-takip && pnpm test tests/26-bildirim.test.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - File tests/26-bildirim.test.ts exists
    - File contains exactly 8 describe blocks with labels BLD-01 through BLD-08
    - `pnpm test tests/26-bildirim.test.ts` exits with non-zero status (tests fail)
    - Output contains "failed" — not "0 tests" (tests must be discovered and failing, not skipped)
    - No TypeScript syntax errors in the file (pnpm typecheck passes on this file or errors are only about missing modules, not syntax)
    - BLD-08 uses `telegramRouter._def.record` pattern matching 25-telegram.test.ts
    - BLD-07 mocks sendTelegramMessage and asserts not called
  </acceptance_criteria>

  <done>tests/26-bildirim.test.ts has 8 RED test cases that will turn GREEN as Waves 1–3 implement the modules</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → module imports | Dynamic imports hit real module resolution; missing modules cause RED failures (expected) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-00-01 | Tampering | test file vi.mock usage | accept | Test-only file; vi.mock is scoped to test runner, no production impact |
| T-26-00-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Wave 0; Vitest already in devDependencies |
</threat_model>

<verification>
pnpm test tests/26-bildirim.test.ts — must exit non-zero with 8 failing tests discovered
</verification>

<success_criteria>
Wave 0 complete when tests/26-bildirim.test.ts exists with 8 RED describe blocks BLD-01..BLD-08, all failing due to missing module exports.
</success_criteria>

<output>
Create `.planning/phases/26-bildirim-sisteminin-iyilestirilmesi/26-00-SUMMARY.md` when done.
</output>
