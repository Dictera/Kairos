---
phase: 26-bildirim-sisteminin-iyilestirilmesi
plan: "03"
type: execute
wave: 3
depends_on:
  - "26-02"
files_modified:
  - components/telegram-bildirim-section.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "TelegramBildirimSection renders a Bildirim Kategorileri subsection with 3 Switch toggle rows"
    - "Each Switch row has aria-label matching its visible label"
    - "Toggling a Switch fires updateToggles mutation with the full 3-boolean payload"
    - "Switches are disabled while updateToggles.isPending"
    - "Toggle section appears above the Bildirim Saatleri section"
    - "A Separator divides the toggle group from the time chips"
  artifacts:
    - path: "components/telegram-bildirim-section.tsx"
      provides: "TelegramBildirimSection with 3 Switch toggle rows + getToggles query"
  key_links:
    - from: "components/telegram-bildirim-section.tsx"
      to: "lib/trpc/routers/telegram.ts"
      via: "trpc.telegram.getToggles.queryOptions() + trpc.telegram.updateToggles.mutationOptions()"
      pattern: "getToggles|updateToggles"
---

<objective>
Wave 3: UI layer. Add 3 Switch toggle rows to TelegramBildirimSection for Günlük Duruşmalar, Günlük Süreler, and Haftalık Özet. Depends on Wave 2 because getToggles and updateToggles procedures must exist before the component can call them.

Purpose: Exposes category toggles (D-05, D-06, D-08) to the user in Ayarlar > Sistem tab.
Output: telegram-bildirim-section.tsx with Bildirim Kategorileri section and Separator.
</objective>

<execution_context>
@D:/sigorta-takip/.claude/get-shit-done/workflows/execute-plan.md
@D:/sigorta-takip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/26-bildirim-sisteminin-iyilestirilmesi/26-CONTEXT.md
@.planning/phases/26-bildirim-sisteminin-iyilestirilmesi/26-UI-SPEC.md
@.planning/phases/26-bildirim-sisteminin-iyilestirilmesi/26-PATTERNS.md
</context>

<interfaces>
From lib/trpc/routers/telegram.ts (Wave 2 output):
  getToggles: protectedProcedure.query() → { telegram_gunluk_durusma_aktif: boolean, telegram_gunluk_sure_aktif: boolean, telegram_haftalik_ozet_aktif: boolean }
  updateToggles: protectedProcedure.input(togglesSchema).mutation() → { ok: true }

From components/ui/switch.tsx (pre-installed):
  import { Switch } from '@/components/ui/switch'
  Props: checked: boolean, onCheckedChange: (checked: boolean) => void, disabled?: boolean, aria-label?: string

From components/ui/separator.tsx (pre-installed):
  import { Separator } from '@/components/ui/separator'

Existing component queries/mutations to keep unchanged:
  const scheduleOpts = trpc.telegram.getSchedule.queryOptions()
  const { data: scheduleData, isLoading } = useQuery(scheduleOpts)
  const updateSchedule = useMutation(trpc.telegram.updateSchedule.mutationOptions(...))
  const testConnection = useMutation(trpc.telegram.testConnection.mutationOptions(...))
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Bildirim Kategorileri toggle section to TelegramBildirimSection</name>
  <files>components/telegram-bildirim-section.tsx</files>

  <read_first>
    - components/telegram-bildirim-section.tsx — full current file: existing imports (lines 1-12), existing query/mutation setup (lines 14-33), existing handler functions (lines 55-69), loading skeleton (lines 71-84), full CardContent render block (lines 86-177) — understand placement: env instruction block, cron times section, Saat Ekle button, Bağlantı Testi button
    - .planning/phases/26-bildirim-sisteminin-iyilestirilmesi/26-UI-SPEC.md — Layout Placement section (toggle group before Bildirim Saatleri), Interaction Contract (div layout, Switch props, aria-label), Copywriting Contract (exact Turkish label and description strings), Typography (font-normal for labels not font-medium), Loading State section
    - .planning/phases/26-bildirim-sisteminin-iyilestirilmesi/26-PATTERNS.md — "telegram-bildirim-section.tsx" section: new import lines, togglesOpts query, updateToggles mutation, Switch toggle row UI pattern, loading skeleton note
    - components/ui/switch.tsx — confirm the Switch component's prop names (checked, onCheckedChange, disabled, aria-label)
  </read_first>

  <action>
    Edit components/telegram-bildirim-section.tsx — do not rewrite the whole file, make targeted additions and insertions:

    1. IMPORTS: Add `import { Switch } from '@/components/ui/switch'` and `import { Separator } from '@/components/ui/separator'` to the existing import block (after the existing UI component imports).

    2. QUERY: After the existing `const { data: scheduleData, isLoading } = useQuery(scheduleOpts)` line, add:
       `const togglesOpts = trpc.telegram.getToggles.queryOptions()`
       `const { data: toggles } = useQuery(togglesOpts)`

    3. MUTATION: After the existing `updateSchedule` mutation block, add the `updateToggles` mutation using `trpc.telegram.updateToggles.mutationOptions`. onSuccess: invalidate togglesOpts.queryKey AND scheduleOpts.queryKey (invalidate both so the card refreshes fully), then `toast.success('Bildirim ayarları güncellendi.')`. onError: `toast.error('Güncelleme başarısız. Lütfen tekrar deneyin.')` — note: different error text from updateSchedule (per UI-SPEC Copywriting Contract).

    4. LOADING SKELETON: In the existing isLoading skeleton branch (CardContent), add two additional skeleton lines after the existing two: `<div className="h-4 w-40 animate-pulse rounded bg-muted" />` and `<div className="h-4 w-36 animate-pulse rounded bg-muted" />` — this covers the toggle section in loading state.

    5. TOGGLE GROUP JSX: Inside the main CardContent, insert the toggle section AFTER the env instruction block div and BEFORE the "Bildirim Saatleri" div. The structure:
       - Outer div: `<div className="space-y-1">`
       - Subsection header: `<p className="text-sm font-semibold">Bildirim Kategorileri</p>` (font-semibold not font-medium per UI-SPEC)
       - 3 toggle rows, one per category. Each row: `<div className="flex items-center justify-between py-1">` containing a `<div>` with `<p className="text-sm font-normal">{label}</p>` and `<p className="text-xs text-muted-foreground">{description}</p>`, then a `<Switch checked={...} onCheckedChange={...} disabled={updateToggles.isPending} aria-label={label} />`
       - Close the outer div

    6. SEPARATOR: After the toggle group div and before the "Bildirim Saatleri" div, insert `<Separator />`.

    Toggle row definitions (exact copy text per UI-SPEC Copywriting Contract):
    - key: 'telegram_gunluk_durusma_aktif', label: 'Günlük Duruşmalar', description: 'Yaklaşan duruşmalar tek mesajda gönderilir.'
    - key: 'telegram_gunluk_sure_aktif', label: 'Günlük Süreler', description: 'Yaklaşan hak düşürücü süreler tek mesajda gönderilir.'
    - key: 'telegram_haftalik_ozet_aktif', label: 'Haftalık Özet', description: 'Her Pazartesi bu haftaki duruşma ve süreler özetlenir.'

    Switch checked value: `(toggles?.[key as keyof typeof toggles]) ?? true` — the `?? true` default means all toggles appear ON while loading (no flicker to OFF state).

    Switch onCheckedChange: `(v) => updateToggles.mutate({ telegram_gunluk_durusma_aktif: toggles?.telegram_gunluk_durusma_aktif ?? true, telegram_gunluk_sure_aktif: toggles?.telegram_gunluk_sure_aktif ?? true, telegram_haftalik_ozet_aktif: toggles?.telegram_haftalik_ozet_aktif ?? true, [key]: v })` — spreads all 3 current values then overrides the changed key.

    Do NOT render toggle rows using Array.map over a config array — instead render each of the 3 rows explicitly (avoids TypeScript key indexing issues with the toggles type).

    The existing Bildirim Saatleri section label currently reads `<p className="text-sm font-medium">Bildirim Saatleri</p>` — change `font-medium` to `font-semibold` to match the new "Bildirim Kategorileri" heading style (UI-SPEC Typography: only 400 and 600 weights used).
  </action>

  <verify>
    <automated>cd D:/sigorta-takip && pnpm typecheck 2>&1 | grep -E "telegram-bildirim|error" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - File contains `import { Switch } from '@/components/ui/switch'`
    - File contains `import { Separator } from '@/components/ui/separator'`
    - File contains `trpc.telegram.getToggles.queryOptions()` call
    - File contains `trpc.telegram.updateToggles.mutationOptions` call
    - File contains `Bildirim Kategorileri` text (subsection header)
    - File contains `Günlük Duruşmalar`, `Günlük Süreler`, `Haftalık Özet` as label text
    - File contains `aria-label` on each Switch (3 occurrences)
    - File contains `<Separator />` between the toggle group and Bildirim Saatleri section
    - File contains `Bildirim ayarları güncellendi.` toast text (success)
    - File contains `Güncelleme başarısız. Lütfen tekrar deneyin.` toast text (error — different from updateSchedule error text)
    - Each Switch has `disabled={updateToggles.isPending}`
    - `?? true` used for all 3 Switch checked defaults
    - pnpm typecheck passes with 0 errors on this file
    - pnpm build passes (no client component errors)
  </acceptance_criteria>

  <done>TelegramBildirimSection shows 3 Switch toggles under Bildirim Kategorileri with Separator; toggles fire updateToggles mutation; disabled during pending; aria-labels present</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All Phase 26 changes: settings-helper.ts (new), notify.ts (grouped messages + toggles), weekly.ts (formalized + durusma join + emoji removed), telegram.ts router (getToggles + updateToggles), TelegramBildirimSection (3 Switch toggles).
  </what-built>
  <how-to-verify>
    1. Run: pnpm dev — server starts without errors
    2. Navigate to http://localhost:3000/ayarlar → Sistem tab
    3. Confirm "Bildirim Kategorileri" subsection appears above "Bildirim Saatleri"
    4. Confirm 3 toggle rows: Günlük Duruşmalar, Günlük Süreler, Haftalık Özet — all ON by default
    5. Toggle one Switch OFF — observe toast "Bildirim ayarları güncellendi."
    6. Reload the page — confirm the disabled toggle remains OFF (persisted to settings.json)
    7. Toggle it back ON — confirm it returns to ON after reload
    8. Run: pnpm test — all tests pass including BLD-01 through BLD-08
    9. Run: pnpm typecheck — 0 errors
  </how-to-verify>
  <resume-signal>Type "approved" if all 9 verification steps pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client Switch → updateToggles tRPC | Boolean values from Switch onCheckedChange cross the HTTP boundary |
| client → getToggles tRPC | Read of toggle state; no sensitive data returned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03-01 | Elevation of Privilege | getToggles / updateToggles UI calls | mitigate | Both procedures use protectedProcedure (Wave 2); unauthenticated users cannot access Ayarlar page (session guard on route) |
| T-26-03-02 | Tampering | Switch onCheckedChange value | mitigate | Zod z.boolean() validation in updateToggles procedure (Wave 2); client-side boolean comes from Switch component (native boolean), not arbitrary user text |
| T-26-03-03 | Information Disclosure | Toggle state visible in UI | accept | Toggle state is non-sensitive (just notification preferences); no PII disclosed |
| T-26-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages in Wave 3; Switch and Separator already installed |
</threat_model>

<verification>
- pnpm test — all tests pass (BLD-01 through BLD-08 GREEN)
- pnpm typecheck — 0 errors
- pnpm build — 0 client component errors
- Manual: Ayarlar > Sistem tab shows 3 toggles, toggle state persists across page reload
</verification>

<success_criteria>
Phase 26 complete when: all 4 plans executed, BLD-01 through BLD-08 GREEN, pnpm test passes, pnpm typecheck passes, 3 Switch toggles functional in Ayarlar UI with persistence to settings.json, grouped daily notification format confirmed, weekly summary has no emoji and includes durusma rows.
</success_criteria>

<output>
Create `.planning/phases/26-bildirim-sisteminin-iyilestirilmesi/26-03-SUMMARY.md` when done.
</output>
